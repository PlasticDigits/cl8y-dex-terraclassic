# Optional repo-root overrides for local infra (copy to `.env`; gitignored).
# Used by deploy, QA, and postgres setup scripts.

POSTGRES_USER=cl8y_legal
POSTGRES_PASSWORD=cl8y_legal
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=dex_indexer
POSTGRES_TEST_DB=dex_indexer_test
