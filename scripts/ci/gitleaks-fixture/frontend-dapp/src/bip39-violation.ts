# Intentional gitleaks fixture — must NOT be copied into real app source.
# Path mirrors frontend-dapp/src so the custom BIP39 rule in .gitleaks.toml fires.
FAKE_DEV_SEED = "abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about"
