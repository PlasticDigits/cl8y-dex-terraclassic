# GitLab #659 — Swap direction seam crops

After shots of `.swap-io-stack` on `/` (disconnected). Viewport in the filename (`375` / `1280`). No wallet identity.

Regenerate: `make verify-issue-659` (Playwright `e2e/swap-direction-seam-659.spec.ts`).
