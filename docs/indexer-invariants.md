# Indexer — Invariants, Business Logic, and Security

This document describes **on-chain indexing** and **read-only HTTP API** behavior for [`indexer/`](../indexer/). It pairs with [contracts-security-audit.md](./contracts-security-audit.md) (on-chain) and [security-model.md](./security-model.md) (protocol).

## Architecture

- **Indexer task:** LCD → parse txs → Postgres (swaps, candles, traders, positions, limit fills and lifecycle rows, liquidity events, hooks, oracle rows).
- **API task:** Axum handlers (primarily `GET`) → SQLx (parameterized) → JSON; `POST /api/v1/route/solve` for hybrid-aware route merge and optional LCD simulation.
- **Shared state:** `AppState` (pool, LCD client, USTC price cache, ticker map cache, orderbook cache, optional `router_address` for route simulation).

## API invariants

| Invariant | Enforcement | Unhappy path | Tests |
|-----------|-------------|--------------|-------|
| Read-only surface | `GET` on all routes except **`POST /api/v1/route/solve`** (JSON body, no writes to indexer DB); CORS allows `GET` and `POST` | Other `POST` → **405** | [`api_route_solve.rs`](../indexer/tests/api_route_solve.rs) |
| No SQL injection on dynamic ordering | Leaderboard `sort` matched to fixed columns in Rust + API allowlist | Unknown `sort` → **400** | [`security.rs`](../indexer/tests/security.rs), [`api_traders.rs`](../indexer/tests/api_traders.rs) |
| Candle `interval` allowlist | `VALID_INTERVALS` | Bad / injection-like string → **400** | `security.rs` |
| Candle default time window | `GET .../candles` without `from`/`to`: **`from` = `to` − 90 days**, **`to` = now** (RFC 3339 bounds on `open_time`) | Short windows (historically 7 days) made charts **empty** when indexed swaps were older than the window (common on LocalTerra / QA) while `trades` still returned rows — see [`pairs.rs`](../indexer/src/api/pairs.rs) `DEFAULT_CANDLE_LOOKBACK_DAYS` | `api_pairs.rs` |
| Numeric query caps | `.clamp(1, max)` on list `limit`/`depth` (e.g. 200, 500, 1000 per route); pair list **`offset` ≤ 10_000** → **400**; token/CG pair lists paginated (default limits, max offset 10_000) | Negative/zero `limit` clamps to **1** (never negative `LIMIT` → **500**); oversized `limit`/`depth` upper-clamped; deep offset → **400** | `security.rs` (pairs candles/trades, oracle history, trader trades), [`api_cg.rs`](../indexer/tests/api_cg.rs), [`api_cmc.rs`](../indexer/tests/api_cmc.rs), [`api_hooks.rs`](../indexer/tests/api_hooks.rs) (`hooks_negative_and_zero_limit_clamp_to_one_not_500`), per-endpoint lower-bound sweep in [`api_limit_lower_bound.rs`](../indexer/tests/api_limit_lower_bound.rs) (every SQL-backed list `limit`) + orderbook `depth=-1/0` in [`api_orderbook_lcd_mock.rs`](../indexer/tests/api_orderbook_lcd_mock.rs), static idiom guardrail [`limit_clamp_guardrail.rs`](../indexer/tests/limit_clamp_guardrail.rs) (fails on any re-introduced `unwrap_or(…).min(…)`), [`indexer_pair_volume_pagination.rs`](../indexer/tests/indexer_pair_volume_pagination.rs); GitLab [**#243**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/243), [**#284**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/284), [**#317**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/317) |
| CG `type` filter | Only `buy`, `sell`, or omit | Invalid → **400** | `api_cg.rs` |
| Ticker / market pair shape | Exactly one `_`, non-empty `BASE` and `TARGET` | `A_B_C`, `_`, `A_`, malformed → **400** | `security.rs` (`cg_ticker_id_attack_matrix`), `api::cg_ticker_tests`, **proptest** on `cg_ticker_segments` |
| Unknown pair / token | DB lookup miss | **404** with short message | Various `api_*.rs` |
| Internal errors sanitized | `internal_err()` → `"Internal server error"` | DB/LCD errors never echo sqlx/SQL | `security.rs` |
| **LCD upstream errors sanitized (H6)** | `lcd_gateway_err()` → `"Upstream LCD query failed"` (**502**); full `LcdError` / URLs only in `tracing` | Limit-book / order-book-head / route hybrid paths must not return `LCD query failed: …` | [`errors.rs`](../indexer/src/api/errors.rs), [`security.rs`](../indexer/tests/security.rs); GitLab [**#239**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/239), [`skills/AGENTS_INDEXER_API_LCD_SECURITY.md`](../skills/AGENTS_INDEXER_API_LCD_SECURITY.md) |
| Hooks errors | Same `internal_err()` as rest of API | No raw DB text | Code path in [`hooks.rs`](../indexer/src/api/hooks.rs) |
| CORS | Allowlist from `CORS_ORIGINS` | Disallowed `Origin` → no `ACA-O` | `security.rs` |
| Abuse: rate limit | Global `tower_governor` when `RATE_LIMIT_RPS > 0` (default **60**; **`RUN_MODE=prod`** forces **60** if unset to **0**). LCD-heavy routes (`limit-book`, `limit-book/insert-hints`, `order-book-head`, **`/cg/orderbook`**, **`/cmc/orderbook/*`**, `route/solve`, `route/solve/best`) also use **`RATE_LIMIT_LCD_HEAVY_RPS`** (default **10**; prod forces **10** if **0**). Keys on **socket peer IP** via `PeerIpKeyExtractor` — no trusted forwarded header in this deployment (`CF-Connecting-IP` / `X-Forwarded-For` absent); a SmartIp/XFF extractor would be spoofable. API listener is **IPv4-only by default** (`API_IPV6_ENABLED` unset/`0`; rejects IPv6 `API_BIND`) to avoid IPv6 `/64` address-rotation abuse ([#282](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/282)) | Sustained burst → **429** | `security.rs`, [`api/mod.rs`](../indexer/src/api/mod.rs); GitLab [**#239**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/239), [**#278**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/278), [**#282**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/282) |
| Observability | **`tracing` logs only** — no `/metrics` endpoint or Prometheus dependency ([GitLab **#200**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/200); supersedes #89 / #125) | N/A | Startup logs in [`main.rs`](../indexer/src/main.rs); block-time warnings in [`block_indexer.rs`](../indexer/src/indexer/block_indexer.rs) |
| Abuse: slow handlers | `TimeoutLayer` 30s | **408** Request Timeout | Documented; no slow-query test |
| Abuse: response size | `CompressionLayer` | Reduces bandwidth cost | Operational |
| LCD amplification | Orderbook cached 30s per `(pair, depth, fee_bps, bid_head, ask_head)`; `depth` = **query total** (Openware), not per-side count. Deep **`limit-book`**: ≤ **101** LCD queries/page ([`LIMIT_BOOK_LCD_QUERY_BUDGET`](../indexer/src/api/limit_book_lcd.rs)). Route **`global_v1`**: ≤ **`LCD_HYBRID_SIM_BUDGET`** pair sims/request ([`best_execution.rs`](../indexer/src/api/best_execution.rs)). Stricter **10 RPS** governor on LCD-heavy routes including CG/CMC orderbook mirrors (**#239**, **#278**). | Repeated CG/CMC hits reuse cache; abuse → **429** on heavy routes | [`orderbook_sim.rs`](../indexer/src/api/orderbook_sim.rs) (**#210**, **#221**, **#220**), [`api_orderbook_lcd_mock.rs`](../indexer/tests/api_orderbook_lcd_mock.rs), [`skills/AGENTS_INDEXER_API_LCD_SECURITY.md`](../skills/AGENTS_INDEXER_API_LCD_SECURITY.md) |
| Hybrid orderbook sim | CG/CMC = **pool curve** + **resting limits** merged per side (`levels_per_side`); `ORDERBOOK_HYBRID=0` → pool-only | Listing QA; not guaranteed fill | [`hybrid_orderbook_sim.rs`](../indexer/src/api/hybrid_orderbook_sim.rs), [`CG_CMC_COMPLIANCE.md`](./CG_CMC_COMPLIANCE.md) § Hybrid Orderbook Simulation; GitLab **#220** |
| Listing orderbook timestamps | `/cg/orderbook` `timestamp` = JSON **number**, Unix **ms**; `/cmc/orderbook/*` = JSON **number**, Unix **s** (not RFC3339 strings; aligns with Kujira CG + CMC trades) | String timestamps break strict listing parsers | [`listing_timestamps.rs`](../indexer/src/api/listing_timestamps.rs), [`api_orderbook_lcd_mock.rs`](../indexer/tests/api_orderbook_lcd_mock.rs); GitLab [**#222**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/222), [**#224**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/224) |
| CMC orderbook shape | `GET /cmc/orderbook/{pair}` → JSON **array** of **exactly one** book object `[{ timestamp, bids, asks }]` (Openware Peatio); CG `/cg/orderbook` stays a **single object** with `ticker_id` | Root object rejected by CMC crawlers; must not wrap CG in an array | [`cmc.rs`](../indexer/src/api/cmc.rs), [`api_cmc.rs`](../indexer/tests/api_cmc.rs), [`CG_CMC_COMPLIANCE.md`](./CG_CMC_COMPLIANCE.md) § Spec matrix; GitLab [**#223**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/223), [**#224**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/224) |
| AMM orderbook sim (pool leg) | `ceil_div` + `fee_bps`; query `depth` ≤ 100 **total** → `levels_per_side = max(1, depth/2)` | **#221** breaking vs per-side depth on other venues | [`CG_CMC_COMPLIANCE.md`](./CG_CMC_COMPLIANCE.md) § AMM Orderbook Simulation, [`skills/AGENTS_INDEXER_AMM_ORDERBOOK_SIM.md`](../skills/AGENTS_INDEXER_AMM_ORDERBOOK_SIM.md) |
| Ticker resolution load | Full ticker→pair map cached 30s | Reduces DB scans on CG/CMC | [`api/mod.rs`](../indexer/src/api/mod.rs) |
| Route discovery | `GET /api/v1/route/solve` without `amount_in` — **first** BFS path over indexed pairs (**default max 3 hops**; **`pool_only=true`** → **max 4 hops**); `token_in` / `token_out` must match `assets.contract_address` (native-only assets without a contract address are not routable) | Unknown token → **400**; no path → **404** | [`route_solver.rs`](../indexer/src/api/route_solver.rs), [`api_route_solve.rs`](../indexer/tests/api_route_solve.rs); GitLab [**#191**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/191) |
| Route GET global best execution | When `amount_in` is set and `pool_only` is not true, GET runs **global best execution** ([GitLab **#209**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/209), [ADR 0002](./adr/0002-global-best-execution-route-solver.md), [route-solver.md](./route-solver.md)): up to **5** simple paths by hop count, **joint** per-hop hybrid grid (17 `book_input` fractions + 2 coordinate passes), pick max `estimated_amount_out` via router `simulate_swap_operations` when configured (**max 3 hops**). Response adds `solver_version`, `paths_considered`, `optimality_scope`, `lcd_hybrid_queries`, `intermediate_tokens`, `quote_kind`, `hybrid_notes`. Cache key includes `solver_version`, `(token_in, token_out, amount_bucket, max_maker_fills, trader\|none, discount_tier)` where **`discount_tier`** is the synced `traders.tier_id` for the discount subject (`trader` when set, else `sender`; unknown → **0**) so same-tier callers share the cache and different tiers cannot collide ([#283](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/283)). Optional query **`trader`** / **`sender`** forwarded to every pair `HybridSimulation` and router sim for CL8Y tier parity ([#245](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/245)); invalid `terra1` address → **400**. Legacy **`pool_only=true`** skips optimization (**max 4 hops**). Deprecated **`hybrid_optimize=true`** without `amount_in` → **400**. | Zero `amount_in` on `/best` → **400**; LCD failure → **502**; router sim error → **400** | `api_route_solve.rs`, [`best_execution.rs`](../indexer/src/api/best_execution.rs), [`route_paths.rs`](../indexer/src/api/route_paths.rs), [`hybrid_route_opt.rs`](../indexer/src/api/hybrid_route_opt.rs), [`skills/AGENTS_INDEXER_HYBRID_BEST_EXECUTION.md`](../skills/AGENTS_INDEXER_HYBRID_BEST_EXECUTION.md) |
| Route GET best execution | `GET /api/v1/route/solve/best` — **alias** for default GET with `amount_in` (same `global_v1` engine). Retail / Vyntrex path ([GitLab **#189**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/189)). | Missing `amount_in` → **400** | `api_route_solve.rs`, [`integrators.md`](./integrators.md) |
| CG/CMC consolidated reporting | `/cg/tickers` and `/cmc/summary` include `cl8y_extensions` with 24h hybrid vs pool-only trade counts and leg volumes. Standard volume fields remain **consolidated** totals from `swap_events` (no double-count with `limit_order_fills`). Trade feeds add optional `pool_leg_volume` / `book_leg_volume` when indexed. | N/A | [`api_consolidated_reporting.rs`](../indexer/tests/api_consolidated_reporting.rs), [`CG_CMC_COMPLIANCE.md`](./CG_CMC_COMPLIANCE.md) |
| CG/CMC aggregator snapshot load | `/cg/tickers`, `/cmc/summary`, `/cmc/ticker` load 24h stats via **set-based** queries (`get_24h_stats_all_pairs` + optional `get_24h_hybrid_breakdown_all_pairs`), not per-pair N+1. Default **`limit=100`** (top pairs by 24h quote volume); optional `limit`/`offset` (offset > 10_000 → **400**). **60s** response cache per endpoint + pagination key. | Stale up to cache TTL | [`aggregator_snapshot.rs`](../indexer/src/api/aggregator_snapshot.rs), [`api_aggregator_batch.rs`](../indexer/tests/api_aggregator_batch.rs); GitLab [**#288**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/288) |
| **L10 — Hybrid volume reconciliation** | Headline volume = one `swap_events` row per taker swap (`offer_amount` / `return_amount`). When `pool_return_amount` and `book_return_amount` are set, **`pool + book = return_amount`** (ask-side). `cl8y_extensions` 24h leg sums are **ask-side attribution**, not additive to `target_volume`. `GET /api/v1/pairs/{addr}/trades` exposes `pool_leg_volume` / `book_leg_volume` (CG/CMC aliases). Do not sum `limit_order_fills` into pair volume when parent swap exists. | NULL legs → omit attribution, not zero headline volume | [`integrators-hybrid-volume.md`](./integrators-hybrid-volume.md), [`api_integrator_hybrid_volume.rs`](../indexer/tests/api_integrator_hybrid_volume.rs), [`swap_events_hybrid_columns.rs`](../indexer/tests/swap_events_hybrid_columns.rs), [`skills/AGENTS_INTEGRATOR_HYBRID_VOLUME.md`](../skills/AGENTS_INTEGRATOR_HYBRID_VOLUME.md); GitLab [**#216**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/216) |
| Route hybrid merge + simulation | `POST /api/v1/route/solve` with JSON `{ token_in, token_out, amount_in?, hybrid_by_hop?, trader?, sender? }`. When `hybrid_by_hop` is set, its length **must equal** the number of hops; each entry is `null` (pool-only hop) or a `HybridSwapParams`-shaped object (`pool_input`, `book_input`, `max_maker_fills`, `book_start_hint`). Response `router_operations` reflects merged `hybrid` fields. Optional `estimated_amount_out` uses LCD `simulate_swap_operations` on the router when `amount_in` and `ROUTER_ADDRESS` are set (router validates leg sums); optional **`trader`** / **`sender`** forwarded ([#245](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/245)). **3-hop POST** with hybrid on multiple legs: `route_solve_post_three_hop_multi_leg_hybrid` ([GitLab **#192**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/192)). | Bad hybrid length → **400**; router/LCD query error → **400** with generic message (no raw LCD stack) | `api_route_solve.rs` |
| Route simulation (GET) | Optional `estimated_amount_out` when `amount_in` is set **and** `ROUTER_ADDRESS` env is configured | LCD `simulate_swap_operations` (pool-only or hybrid-merged ops) | Same; requires live LCD in production |
| Pair liquidity history | `GET /api/v1/pairs/{addr}/liquidity-events` — `limit`/`before` capped like trades | Unknown pair → **404** | [`api_pairs.rs`](../indexer/tests/api_pairs.rs) |
| Limit placements / cancellations | `GET .../limit-placements` omits `(pair_id, order_id)` present in `limit_order_cancellations`; each row includes **`lifecycle_status`** (`active`, `parked_expired`, `refunded`) with **`remaining_escrow`** when parked ([GitLab **#142**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/142)); default listing is **`active` + `parked_expired`** (excludes **`refunded`**); **`?status=`** filters `active` \| `parked_expired` \| `refunded` \| `all`; `.../limit-cancellations` lists indexed cancels | Unknown pair → **404**; bad **`status`** → **400** | [`api_pairs.rs`](../indexer/tests/api_pairs.rs), [`limit_order_parked_lifecycle.rs`](../indexer/tests/limit_order_parked_lifecycle.rs); [GitLab **#135**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/135), [**#142**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/142) |
| **Trader history (swaps, limit fills, cancels)** | `GET /api/v1/traders/{addr}/trades` — optional **`pair=`** pair contract filter (**404** unknown pair); optional **`format=csv`** → **`text/csv`** attachment (UTF-8) with stable header row; default **`format=json`**. **`GET .../limit-fills`** — rows where indexed **`maker`** equals `{addr}`; optional **`pair=`** filters to that pair. **`GET .../limit-cancellations`** — rows where indexed **`owner`** equals `{addr}`; optional **`pair=`** filter. Same **`format`**, **`limit`**, **`before`** caps (**`limit` ≤ 200**). Swap JSON adds optional **`commission_amount`** / **`spread_amount`** when indexed. | Bad **`format`** (not `json` or `csv`) → **400** | [`api_traders.rs`](../indexer/tests/api_traders.rs), [`security.rs`](../indexer/tests/security.rs); [GitLab **#163**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/163); [`docs/frontend.md` § Wallet swap and limit history](./frontend.md#wallet-swap-limit-history), [`skills/AGENTS_FRONTEND_ORDER_HISTORY.md`](../skills/AGENTS_FRONTEND_ORDER_HISTORY.md) |
| **Trader open limit placements** | `GET /api/v1/traders/{addr}/limit-placements` — wallet-wide resting limits: same cancel omission and **`lifecycle_status`** / **`?status=`** semantics as pair-scoped **`GET .../pairs/{addr}/limit-placements`**; filters rows where indexed **`owner`** equals `{addr}` (legacy placements without `owner` are omitted). Optional **`pair=`** scopes to one pair (**404** unknown). **`limit` ≤ 200**, **`before`** cursor on placement `id`. No CSV. | Bad **`status`** → **400**; unknown **`pair=`** → **404** | [`api_traders.rs`](../indexer/tests/api_traders.rs) (`get_trader_limit_placements_returns_owner_rows`); dApp: [`docs/frontend.md` § My Portfolio](./frontend.md#my-portfolio), [GitLab **#217**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/217), [`skills/AGENTS_FRONTEND_PORTFOLIO.md`](../skills/AGENTS_FRONTEND_PORTFOLIO.md) |
| **Trader open positions** | `GET /api/v1/traders/{addr}/positions` — per-pair quote exposure (`net_position_quote`, `avg_entry_price`, `total_cost_base`, `realized_pnl`, `trade_count`). Indexer-derived on swap ingestion ([`position_tracker.rs`](../indexer/src/indexer/position_tracker.rs)); **not** on-chain balances or unrealized mark-to-market. Empty **`[]`** for flat wallets; profile row may still **404**. | N/A (read-only) | [`api_traders.rs`](../indexer/tests/api_traders.rs) (`get_trader_positions_returns_rows`); dApp: [`docs/frontend.md` § My Portfolio](./frontend.md#my-portfolio), [GitLab **#212**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/212), [`skills/AGENTS_FRONTEND_PORTFOLIO.md`](../skills/AGENTS_FRONTEND_PORTFOLIO.md) |
| On-chain book (LCD proxy) | `GET /api/v1/pairs/{addr}/order-book-head`, `.../limit-book` (paginated; optional `price_from`/`price_to` window), `.../limit-book/insert-hints` (≤ **100** prices), `.../limit-book-shallow` (legacy preview) | Unknown pair → **404**; LCD failure → **502**; `limit-book` `limit` clamped (max 100); insert-hints oversized list / invalid price → **400**; `pagination_gap` when walk cannot reach slot (**#267**); `limit-book-shallow` `depth` clamped (max 20); bad book cursor / side → **400** | [`api_limit_book_lcd_mock.rs`](../indexer/tests/api_limit_book_lcd_mock.rs), [`api_limit_book_deep.rs`](../indexer/tests/api_limit_book_deep.rs), [`api_limit_book_insert_hints.rs`](../indexer/tests/api_limit_book_insert_hints.rs), [`limit-orders.md`](./limit-orders.md), [GitLab **#194**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/194), [GitLab **#267**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/267), [`skills/AGENTS_FRONTEND_DEEP_ORDER_BOOK.md`](../skills/AGENTS_FRONTEND_DEEP_ORDER_BOOK.md) |
| Hooks OpenAPI | `GET /api/v1/hooks` documented under Swagger **Hooks** tag | Same error handling as other read routes | [`api_hooks.rs`](../indexer/tests/api_hooks.rs) |

### Local dev CORS: `localhost` vs `127.0.0.1` {#local-dev-cors-localhost-vs-127001}

The browser sends `Origin` using the hostname from the address bar. `http://localhost:5173` and `http://127.0.0.1:5173` are different origins; if `CORS_ORIGINS` lists only one of them, fetches from the other fail CORS even when the indexer returns **200**. Include both spellings (and Vite preview ports if used) in dev — see [`indexer/.env.example`](../indexer/.env.example), [`scripts/deploy-dex-local.sh`](../scripts/deploy-dex-local.sh), and [GitLab **#131**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/131). After placing a limit, the dApp polls `limit-placements`; failures are logged as **`[limit-place] indexer poll failed:`** via [`warnIndexerPlacementPollFailed`](../frontend-dapp/src/utils/warnIndexerPlacementPollFailed.ts). The **`/trade`** ticket’s **View order** affordance scrolls to indexed placement rows keyed by `order_id` ([GitLab **#161**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/161); [`docs/frontend.md` § Trade page — limit place success affordances](./frontend.md#trade-page-limit-place-success-affordances)).

## Frontend expectations (read path)

The indexer may legitimately return **empty candle arrays** for a pair/interval with no buckets yet. The dApp must treat that as **success**, not a silent chart failure. UI invariants and lightweight-charts vs TradingView widget naming are documented under [Trade page — price chart invariants](./frontend.md#trade-page--price-chart-invariants) (GitLab [**#113**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/113), [**#150**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/150)).

When validating LocalTerra flows with **browser wallets**, fee broadcast quirks are **frontend/wallet** concerns — on **LocalTerra** use **Terra Classic Keplr** or the **dev/simulated wallet** for gas/fee verification; **do not use Terra Station** (built-in stale **`localterra`** gas step — not fixable by dApp overrides alone; [GitLab **#235**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/235), [`docs/frontend.md` § Station extension signing](./frontend.md#station-extension-signing)). **Terra Station P0** belongs on **columbus-5**. Mitigations still in code for columbus-5 / when Station fixes LocalTerra: **`addNetwork` gas refresh**, **`experimentalSuggestChain`**, **per-sign `preferNoSetFee` + post-sign guard vs `stdDoc.fee`** in patched cosmes **`KeplrExtension`**, **Station extension → amino signing** in patched **`StationController`**, **min gas price**, **`npm ci` / `cosmesPatch127.test.ts`** — [`docs/frontend.md` § Terra Classic gas limits](./frontend.md#terra-classic-gas-limits), GitLab [**#127**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/127)); the indexer only observes txs once included on-chain.

**Pair pause vs limit claim:** On-chain **`ClaimExpiredLimitOrder`** is rejected while the pair is paused (same **`assert_not_paused`** gate as cancel — [contracts-security-audit.md](./contracts-security-audit.md) **L6**, GitLab [**#120**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/120)). Bots and UIs must not broadcast claim txs during a pause; the dApp mirrors this on **Claim refund** ([`skills/AGENTS_FRONTEND_LIMIT_PARKED_EXPIRED.md`](../skills/AGENTS_FRONTEND_LIMIT_PARKED_EXPIRED.md)).

**Router `simulate_swap_operations` vs spread:** Route solve responses (and the dApp’s matching wallet LCD `simulate_swap_operations` query) carry **router_operations** plus an optional **`estimated_amount_out`**, but the router simulation payload does **not** include per-hop **spread** — only the final amount. The retail Swap page therefore runs a **sequential pair-level preflight** (factory + `simulation` / `hybrid_simulation`) so **price impact** and submit gating align with on-chain **`max_spread`** per hop. **Route preview:** when both the indexer graph and the client BFS can describe the same hops, the UI must not show duplicate “indexer vs client” route lines — the path shown must follow the operations used at submit ([GitLab **#158**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/158), [`skills/AGENTS_FRONTEND_SWAP_ROUTE_DISPLAY.md`](../skills/AGENTS_FRONTEND_SWAP_ROUTE_DISPLAY.md)). Invariants and agent notes: [swap max spread / price impact UX](./swap-max-spread-ux.md) (GitLab [**#134**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/134)), [`docs/frontend.md` § Swap Page Integration](./frontend.md#swap-page-integration), [`skills/AGENTS_LOCALNET_TRADING_SWARM.md`](../skills/AGENTS_LOCALNET_TRADING_SWARM.md).

## Indexing invariants

| Invariant | Enforcement | Unhappy path | Tests |
|-----------|-------------|--------------|-------|
| Block time usable | Tx RFC3339 first; else LCD **block header** `time`; **no** `Utc::now()` fallback — missing/invalid both → block ingest **fails** (cursor unchanged) | Misaligned candles avoided; operator fixes LCD | [`block_indexer.rs`](../indexer/src/indexer/block_indexer.rs) `resolve_block_time`; [`indexer_ingestion_hardening.rs`](../indexer/tests/indexer_ingestion_hardening.rs); GitLab [**#243**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/243) |
| Pair list 24h volume | `pair_volume_24h` table refreshed ~5 min by [`volume_aggregator`](../indexer/src/indexer/volume_aggregator.rs); pair list JOINs rollup (not live 24h scan) | Stale sort up to refresh interval (~5 min) | [`indexer_pair_volume_pagination.rs`](../indexer/tests/indexer_pair_volume_pagination.rs); migration `20260531143000_*`; [**#243**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/243), [`skills/AGENTS_INDEXER_VOLUME_PAGINATION.md`](../skills/AGENTS_INDEXER_VOLUME_PAGINATION.md) |
| Swap dedup | Unique index on `(tx_hash, pair_id, swap_index)`; `INSERT ... ON CONFLICT DO NOTHING` in [`insert_swap`](../indexer/src/db/queries/swap_events.rs). `swap_index` is a 0-based per-pair ordinal within the tx (parser walk order) so multiple swaps on the same pair in one tx are distinct rows; replay of the same swap is still deduped. | Replay skipped; optional `trade_exists` fast path in parser | Migration `20260605000000_*` (widens `20260326120000_*`); [`parser.rs`](../indexer/src/indexer/parser.rs) `parse_swaps_assigns_per_pair_swap_index`; [`limit_fill_swap_linkage.rs`](../indexer/tests/limit_fill_swap_linkage.rs); GitLab [**#287**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/287) |
| Limit placement lifecycle | Parser applies **`limit_order_expired_parked`** (`remaining`) then **`claim_expired_limit_order`** on `limit_order_placements`; transitions **`active` → `parked_expired` → `refunded`**. Parsers scan **every** `action` key in each `wasm` event (not only `wasm_attr_last`), so a flattened stream where **`action=swap` is last** still records an earlier **`limit_order_expired_parked`** ([GitLab **#141**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/141)). | Replay idempotent (updates match at-most-one row per event) | [`parser.rs`](../indexer/src/indexer/parser.rs); [`limit_order_lifecycle.rs`](../indexer/src/db/queries/limit_order_lifecycle.rs); [`limit_order_parked_lifecycle.rs`](../indexer/tests/limit_order_parked_lifecycle.rs); GitLab [**#142**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/142), [**#141**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/141) |
| **Limit fill rows** | Hybrid swaps emit one on-chain **`limit_order_fill`** wasm event per maker. Parser scans **every** `action=limit_order_fill` in each `wasm` / `wasm-wasm` event (same merged-stream pattern as lifecycle #141), not `wasm_attr_last(action)`. Inserts one `limit_order_fills` row per maker; dedup **`UNIQUE (tx_hash, pair_id, order_id)`** + `fill_exists`. Optional `swap_event_id` links the parent `swap_events` row by the fill's per-pair **`swap_index`** (deterministic parser walk ordinal — maker fills precede their `swap` action in the same execute), so a tx with multiple swaps on one pair links each fill to the swap that produced it, not the first by id ([`swap_id_for_tx_pair_index`](../indexer/src/db/queries/limit_order_fills.rs), GitLab [**#316**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/316), follow-up to [**#287**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/287)). Headline pair volume stays on `swap_events` only (**L10**). | Malformed fill segment skipped; unknown pair → discover or skip without failing block | [`parser.rs`](../indexer/src/indexer/parser.rs) unit tests (`parse_limit_order_fills_*`, `parse_limit_order_fills_assigns_swap_index_per_pair_swap`); [`limit_fill_swap_linkage.rs`](../indexer/tests/limit_fill_swap_linkage.rs); [`limit_order_fills.rs`](../indexer/src/db/queries/limit_order_fills.rs); [`api_traders.rs`](../indexer/tests/api_traders.rs); GitLab [**#269**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/269) (follow-up to [**#254**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/254)) |
| Limit placement HTTP feed | `list_placements_for_pair` SQL excludes rows whose `(pair_id, order_id)` exists in `limit_order_cancellations` | Stale UI listing already-cancelled ids as “active” | [`limit_order_lifecycle.rs`](../indexer/src/db/queries/limit_order_lifecycle.rs); `api_pairs.rs`; GitLab [**#135**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/135) |
| Wasm attributes | `wasm_attr_last`: duplicate keys → last wins (swap / pool attrs). **Limit lifecycle** (`limit_order_expired_parked`, `claim_expired_limit_order`) and **`limit_order_fill`**: segment scan per `action` occurrence — see rows above (#141, #269) | Matches CosmWasm + flattened LCD wasm streams | [`parser.rs`](../indexer/src/indexer/parser.rs) unit tests + fuzz |
| Hybrid swap columns | Parser maps optional wasm attrs `pool_return_amount`, `book_return_amount`, `limit_book_offer_consumed`, `effective_fee_bps` onto `swap_events` (hybrid txs). API/CSV expose the same fields on trade rows plus `pool_leg_volume` / `book_leg_volume` aliases. Leg sum invariant documented in [**L10**](#api-invariants). | Missing attrs → NULL columns (pool-only swaps) | [`swap_events_hybrid_columns.rs`](../indexer/tests/swap_events_hybrid_columns.rs); [`integrators-hybrid-volume.md`](./integrators-hybrid-volume.md); parser unit tests; GitLab [**#82**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/82), [**#216**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/216), epic [**#199**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/199); [`skills/AGENTS_TESTING_P2_EPIC.md`](../skills/AGENTS_TESTING_P2_EPIC.md) |
| Candle price positive | `price <= 0` → skip update | No zero/negative OHLC from bad ratio | [`candle_skip_zero_price.rs`](../indexer/tests/candle_skip_zero_price.rs), `merge_candle_ohlc` unit tests |
| Candle OHLC consistency | `high ≥ low`, `high ≥ close`, `low ≤ close`, `open` unchanged on update | Enforced by merge logic | Unit tests + **proptest** on `merge_candle_ohlc` ([`candle_builder.rs`](../indexer/src/indexer/candle_builder.rs)) |
| Position net quote | After sell leg, `net_position_quote` clamped to ≥ 0 | Oversell does not leave negative inventory | [`position_tracker_clamp.rs`](../indexer/tests/position_tracker_clamp.rs), `net_quote_after_sell` unit tests |
| Oracle storage | Non-finite `f64` → safe `BigDecimal` default | No NaN/Inf in DB from conversion | [`oracle.rs`](../indexer/src/indexer/oracle.rs) unit tests |
| **Book snapshot mirror** | [`book_snapshot`](../indexer/src/indexer/book_snapshot.rs) loop fills `pair_reserves` + `resting_limit_orders` on cadence `BOOK_SNAPSHOT_INTERVAL_MS` (default 10s); per-pair LCD failure → warn + skip (last good row kept); staleness TTL `BOOK_SNAPSHOT_MAX_STALENESS_MS` for Phase 1c | Missing/stale mirror → Phase 1c degrades, not error | [`book_snapshot_loop.rs`](../indexer/tests/book_snapshot_loop.rs), [`db_orderbook_mirror.rs`](../indexer/tests/db_orderbook_mirror.rs); [runbook](./runbooks/book-snapshot-mirror.md); GitLab [**#322**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/322) |
| **Cursor commit (C1)** | `last_indexed_height` and `last_indexed_block_hash` advance **only** after successful block ingest ([`block_indexer.rs`](../indexer/src/indexer/block_indexer.rs)); `process_block_txs` **propagates** per-tx errors (no warn-and-skip) | Parser/LCD/DB failure → retry with backoff; after `BLOCK_PROCESS_MAX_RETRIES` halt catch-up; row in `indexer_failed_blocks` | [`indexer_ingestion_hardening.rs`](../indexer/tests/indexer_ingestion_hardening.rs); GitLab [**#236**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/236), [`skills/AGENTS_INDEXER_INGESTION_HARDENING.md`](../skills/AGENTS_INDEXER_INGESTION_HARDENING.md) |
| **Block tx pagination (C2)** | `get_block_txs` loops `search_txs` until `pagination.total` satisfied; rejects incomplete totals; bounded by `BLOCK_TX_MAX_PAGES` × `BLOCK_TX_PAGE_LIMIT` | Busy blocks (>100 txs) fully ingested; pagination DoS fails safe | `lcd/mod.rs` wiremock tests + `indexer_ingestion_hardening.rs`; **#236** |
| **Reorg hash guard (C3)** | Before indexing height `H+1`, re-fetch block `H` hash vs `last_indexed_block_hash`; mismatch → halt (no silent forward progress) | Operator rewind via [`scripts/indexer-reorg-recover.sh`](../scripts/indexer-reorg-recover.sh) + runbook | `indexer_ingestion_hardening.rs`; **#236**, [runbook](./runbooks/indexer-reorg-replay-dedup.md) |
| **Factory pair provenance (P1)** | Opportunistic [`discover_new_pair`](../indexer/src/indexer/pair_discovery.rs) (parser path) queries the pair contract, then **factory `Pair { asset_infos }`** and requires `contract_addr` match. Scheduled [`sync_all_pairs`](../indexer/src/indexer/pair_discovery.rs) is unchanged (factory paginated list). Empty `FACTORY_ADDRESS` → warn + skip provenance (**dev only**); `RUN_MODE=prod` requires non-empty factory ([`config.rs`](../indexer/src/config.rs)). Combined with wasm **`_contract_address`** scoping ([#285](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/285)), clone contracts that mimic the pair query API are not indexed. | Malicious / unlisted pair → discover fails; parser skips event without failing block | [`pair_discovery.rs`](../indexer/src/indexer/pair_discovery.rs) unit tests (`verify_factory_provenance_*`); GitLab [**#311**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/311) (follow-up to [**#285**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/285)) |

## Attack paths (off-chain API)

1. **Query injection via `sort` / `interval`** — Blocked by allowlists (no string pasting into SQL for those).
2. **Ticker confusion / extra underscores** — Blocked by strict `BASE_TARGET` shape (exactly two non-empty segments).
3. **DoS: huge limits** — Blocked by caps on trades, candles, hooks, oracle history, CG trades, orderbook depth.
4. **DoS: expensive DB per request** — Ticker map cache reduces repeated full scans for CG/CMC.
5. **DoS: LCD hammering via orderbook** — Short-TTL cache on simulated orderbook.
6. **DoS: connection exhaustion** — Rate limit + timeout (defense in depth; edge still needs WAF/reverse proxy).
7. **Information leak via errors** — `internal_err` for DB paths; **`lcd_gateway_err`** for LCD proxy paths (**#239** / H6).
8. **LCD amplification abuse** — Per-request query budgets + **`RATE_LIMIT_LCD_HEAVY_RPS`** on limit-book, CG/CMC orderbook mirrors, and route solve (**#239** / H7, **#278**).
9. **CORS token theft** — Browser enforces origin; server only echoes allowlisted origins.

## Postgres stack prerequisite (PG-1, GitLab #245)

| ID | Invariant |
|----|-----------|
| **PG-1** | Indexer runtime and integration tests connect as **`cl8y_legal`** on `dex_indexer` / `dex_indexer_test`. Compose creates the role on a fresh volume. External Postgres with only **`postgres:postgres`** must run [`scripts/setup-postgres-dev-databases.sh`](../scripts/setup-postgres-dev-databases.sh) (superuser bootstrap) or manual role creation — see [`skills/AGENTS_LOCAL_POSTGRES_DEV.md`](../skills/AGENTS_LOCAL_POSTGRES_DEV.md). Regression: `make test-setup-postgres`. |

## Running tests

- **Library tests (no Postgres):** `cd indexer && cargo test --lib` — includes parser/candle/oracle fuzz-style tests, **proptest** on `merge_candle_ohlc` and `cg_ticker_segments`, and invariant unit tests.
- **Integration tests:** Require PostgreSQL and migrations (e.g. CI service or `TEST_DATABASE_URL`). Default local credentials are **`cl8y_legal` / `cl8y_legal`** on `dex_indexer_test` — see agent playbook [`skills/AGENTS_LOCAL_POSTGRES_DEV.md`](../skills/AGENTS_LOCAL_POSTGRES_DEV.md) (stack prerequisite **PG-1**, superuser bootstrap [GitLab **#245**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/245)) and [Testing — shared Postgres](./testing.md#shared-postgres-and-test-parallelism). `cd indexer && cargo test --tests -j 1 -- --test-threads=1`. Orderbook routes are also covered with a **wiremock** stub of the LCD `pool` smart query ([`tests/common/lcd_mock.rs`](../indexer/tests/common/lcd_mock.rs)).

Integration tests **fail fast** if the database is unreachable (see [`tests/common/mod.rs`](../indexer/tests/common/mod.rs)).

**Shared DB / flaky tests:** If you see sporadic duplicate-key or FK errors against one database, run tests with serialized parallelism as documented in [Testing — Shared Postgres and test parallelism](./testing.md#shared-postgres-and-test-parallelism) (`cargo test --tests -j 1 -- --test-threads=1`).

## Block time resolution (GitLab #243)

Block timestamps for indexing come from the LCD transaction response (`tx_responses[0].timestamp`) when valid RFC3339. In [`indexer/src/indexer/block_indexer.rs`](../indexer/src/indexer/block_indexer.rs), `resolve_block_time`:

- Parses tx timestamp into UTC when valid.
- If the tx timestamp is **missing** or **invalid**, retries using the LCD **block header** `time` for that height.
- If **both** are unusable, block ingest **fails** (cursor unchanged) — **no** `Utc::now()` wall-clock fallback.

**Risk mitigated:** Event times and candle bucket boundaries no longer skew to ingestion wall clock when LCD tx metadata is bad but header time is good.

- Run a **reliable LCD** close to your chain; monitor logs for `Invalid tx timestamp` / `Missing tx timestamp` / `Block header time missing or invalid`.
- After prolonged LCD issues, consider **re-indexing** from a known height (see [runbook: reorg / replay / dedup](./runbooks/indexer-reorg-replay-dedup.md)).

## Pair list 24h volume rollup (GitLab #243)

`GET /api/v1/pairs?sort=volume_24h` reads from materialized table **`pair_volume_24h`**, refreshed every **~5 minutes** by the indexer background task (same loop as token volume stats). Expect up to one refresh interval of lag vs live swap totals. Migration: `indexer/migrations/20260531143000_pair_volume_24h_rollup.sql`.

## Dapp read model (pools list)

The Liquidity pool browser (`/pool`) **lists and sorts** pairs from the indexer API; it **tags** each row against the on-chain factory pair set used for the swap router graph. Counts and badges are documented in [Frontend guide — Liquidity pools list (indexer vs factory)](./frontend.md#liquidity-pools-list-indexer-vs-factory) (see [glab#112](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/112)).

### Factory LCD: pair enumeration vs governance gas (agents)

When the indexer (or any tool) syncs pairs from the factory via LCD, use **paginated** `Pairs` / `GetPairCount` and **avoid** assuming unbounded factory queries are cheap on-chain—the factory keeps a sequential `pair_index` for **O(limit)** page reads, while **`start_after`** (asset pair cursor) resolves in **O(1)** via `pair_key_idx` ([GitLab **#258**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/258); see [Smart contract reference — Factory storage & upgrades](./contracts-terraclassic.md#factory-storage--upgrades)). That design is separate from **per-pair governance** messages (`SetPairFee`, `SweepPair`, etc.), which must use an **O(1)** reverse map (`pair_addr_reg`) so gas stays bounded at high pair counts ([GitLab **#122**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/122)). Third-party agents should follow the same split: iterate for **listing** only; never reintroduce linear “is this pair registered?” scans in contract code paths that validate a single address—see [`skills/AGENTS_TERRACLASSIC_GAS.md`](../skills/AGENTS_TERRACLASSIC_GAS.md).

## Maintenance

When adding a new query parameter that influences SQL or ordering:

1. Use an allowlist or parameterized query only.
2. Cap numeric inputs.
3. Add a **happy** and **unhappy** test (400/404/429 as appropriate).
4. Update this matrix if the invariant is user-visible.
