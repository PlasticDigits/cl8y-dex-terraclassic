# CL8Y DEX — Contracts Security Audit (Repository)

This document is the **in-repo security audit and invariant matrix** for the CosmWasm contracts under `smartcontracts/`. It complements [security-model.md](./security-model.md) (operational trust assumptions), [security-posture.md](./security-posture.md) (public launch posture linked from the dApp footer), [exploit-replay-matrix.md](./exploit-replay-matrix.md) (historical Cosmos/CosmWasm incident replay — SEC-D01 / [#406](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/406)), and [testing.md](./testing.md) (how to run tests and coverage).

## Trust model (explicit)


| Assumption                  | Implication for findings                                                                                                                                                                                        |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Governance is honest**    | Multisig/DAO-controlled governance is trusted not to sabotage the protocol (malicious hooks, fee changes, pauses) on purpose. The audit still verifies **non-governance callers cannot** perform admin actions. |
| **Treasury is a sink**      | Commission tokens are transferred to `treasury`; misconfiguration of treasury address is an ops risk, not an unauthorized-access bug.                                                                           |
| **CW20 code ID whitelist**  | Only whitelisted token **code IDs** may back pairs; malicious token *logic* inside a whitelisted template remains a product risk (e.g. fee-on-transfer), mitigated by [CW20 whitelist runbook](./runbooks/cw20-whitelist-policy.md) and tests. **Do not whitelist fee-on-transfer templates.** |
| **Hooks are opt-in policy** | Governance may register hooks that **revert**; by design that blocks swaps ([security-model.md](./security-model.md)).                                                                                          |
| **External git deps**       | `treasury`, `wrap-mapper`, `cw20-mintable` (see [smartcontracts/Cargo.toml](../smartcontracts/Cargo.toml)) are part of the trust boundary for wrap/treasury scenarios in tests and any integrated deployment.   |


## Contract inventory


| Crate / path                                       | Role                                                                                                              |
| -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `cl8y-dex-factory`                                 | Pair creation, whitelist, governance-only pair config (fees, hooks, discount registry, pause, sweep). **Discount registry rollout:** bounded `SetDiscountRegistryBatch` for large pair counts; `SetDiscountRegistryAll` capped at ≤10 pairs ([#242](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/242), [contracts-terraclassic.md § rollout](./contracts-terraclassic.md#factory-discount-registry-rollout-invariants-glab-123), [#123](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/123)).             |
| `cl8y-dex-pair`                                    | Constant-product AMM, reserves, fees, TWAP observations, post-swap hooks, pause/sweep, **FIFO limit book** + Pattern C hybrid swaps. |
| `cl8y-dex-router`                                  | Multi-hop swaps, `SwapState` + reply chain, `trader` forwarding for discounts, optional unwrap via `wrap_mapper`. |
| `cl8y-dex-fee-discount`                            | Tiered discounts, EOA registration, trusted routers, lazy deregistration.                                         |
| `cl8y-dex-burn-hook` / `tax-hook` / `lp-burn-hook` | Post-swap callbacks; gated by **allowed caller** (expected: real pair contracts).                                 |
| `dex-common`                                       | Shared types, oracle math, hook wire format.                                                                      |


## Invariant matrix

Each row states a property that should **always** hold (under the trust model). **Test** column points to the primary automated evidence in `smartcontracts/tests/`.


| ID  | Invariant                                                                                                                                           | Code / notes                                                                                           | Tests / fuzz                                                                                                                                         |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1  | **k monotonicity** — after a swap, k' \geq k within documented rounding bounds                                                                      | [pair `execute_swap](../smartcontracts/contracts/pair/src/contract.rs)`                                | `audit_invariant_tests::p1_k_non_decreasing_after_swap`; `fuzz_tests` / `additional_fuzz_tests` / `security_tests` (K checks); [pair `lib.rs`](../smartcontracts/contracts/pair/src/lib.rs) |
| P2  | **Reserves vs balances** — internal `RESERVES` update only via protocol messages; direct CW20 transfers can desync balance vs reserves              | Documented in pair `lib.rs`                                                                            | `adversarial_token::fee_on_transfer_creates_reserve_imbalance`; `sweep_tests` (excess recovery)                                                                                       |
| P3  | **First-deposit LP lock** — `MINIMUM_LIQUIDITY` burned on first mint; **bootstrap decimals gate** (`MAX_PAIR_ASSET_DECIMALS_BOOTSTRAP` = **18**) on factory `CreatePair` and empty-pool `provide_liquidity` rejects asset CW20s with **`decimals > 18`** ([#124](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/124)) | [pair `execute_provide_liquidity`](../smartcontracts/contracts/pair/src/contract.rs); [factory `create_pair`](../smartcontracts/contracts/factory/src/contract.rs); LP CW20 `decimals` = **`LP_TOKEN_DECIMALS` (18)** (`dex-common`) | `pair_coverage_tests::test_first_deposit_minimum_liquidity`, `test_first_deposit_below_minimum_rejected`, `test_lp_token_instantiated_with_lp_token_decimals_constant`, `test_create_pair_rejects_cw20_above_bootstrap_decimal_cap`, `test_empty_pool_provide_rejects_asset_decimals_above_bootstrap_cap`; `security_tests::test_lp_share_inflation_first_depositor_griefing` |
| P4  | **Fee bounds** — `fee_bps` / effective fee in valid range; commission rounding sane. **Ask-side book fills:** commission on limit matches is taken on the **token0** output leg (not token1 `cost`)                                                                 | Pair fee + discount composition; [orderbook `match_asks`](../smartcontracts/contracts/pair/src/orderbook.rs)                                                                        | `fee_discount_coverage_tests`, integration swap tests, `fee_math_property_tests` (proptest); `limit_order_tests::hybrid_ask_non_unity_price_treasury_fee_in_token0`                                                          |
| P10 | **Commission treasury atomicity (SEC-C03)** — swap commission is transferred to `treasury` in the **ask asset** during settlement; if the swap reverts (max_spread, deadline, hook error, etc.), commission treasury balances are unchanged — no fee retention on the pair ([#402](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/402)) | [pair `execute_swap`](../smartcontracts/contracts/pair/src/contract.rs); [security-model.md § Treasury](./security-model.md#treasury-management) | `audit_invariant_tests::commission_treasury_unchanged_after_max_spread_rejected_swap`, `commission_treasury_unchanged_after_deadline_rejected_swap`; hook revert treasury snapshot: `new_feature_tests::test_state_consistent_after_hook_revert`; successful accumulation: `test_fees_accumulate_across_multiple_swaps` |
| P5  | **Discount registry failure → full fee (fail-closed)** — if `GetDiscount` query fails (registry paused, migrating, or LCD/querier error), pair and hybrid simulation use **configured pair `fee_bps`** with **no** discount metadata; swap still succeeds. **No on-chain revert** and no commission/treasury accounting change without an explicit ADR ([#365](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/365)). Off-chain: indexer `GET /api/v1/health/fee-discount` + dApp non-blocking warning when registry LCD is unreachable. Integrator decision table: [integrators.md § Fee-discount registry outage](./integrators.md#fee-discount-registry-outage). | [pair `discount_cache.rs` `lookup_effective_fee_bps_*` `Err(_) => (fee_bps, None)`](../smartcontracts/contracts/pair/src/discount_cache.rs); [pair `execute_swap`](../smartcontracts/contracts/pair/src/contract.rs) | `audit_invariant_tests::swap_uses_full_fee_when_discount_registry_query_fails`; [`skills/AGENTS_FEE_DISCOUNT_TIERS.md`](../skills/AGENTS_FEE_DISCOUNT_TIERS.md) § Registry outage observability; `make verify-issue-365` |
| P6  | **Untrusted router cannot steal discount** — `trader` only applies when sender is trusted router                                                    | [fee-discount `GetDiscount](../smartcontracts/contracts/fee-discount/src/contract.rs)`                 | `fee_discount_coverage_tests::test_query_discount_untrusted_router_falls_back_to_sender`                                                             |
| P7  | **Factory-only pair admin** — fee, hooks, registry, pause, sweep on pair only from factory                                                          | Pair `execute_*` sender checks                                                                         | `audit_invariant_tests::p7_non_factory_cannot_update_pair_fee`; `line_coverage_tests`, `pair_coverage_tests`, `factory_coverage_tests`                                                                              |
| P8  | **Governance-only factory admin**                                                                                                                   | [factory `ensure_governance](../smartcontracts/contracts/factory/src/contract.rs)`                     | `audit_invariant_tests::p8_non_governance_cannot_add_whitelisted_code_id`; `factory_coverage_tests` (unauthorized)                                                                                                   |
| P9  | **Fee-discount cache is a 300s snapshot (accepted design)** — the pair memoizes the registry's `(effective_fee_bps, discount)` for **`DISCOUNT_CACHE_TTL_SECONDS` (300s)** keyed on `(trader, sender)` and serves it without re-querying within the window (the registry itself reads live balance — see P5/P6). Consequence: a tier holder must transact **≥ once per 300s** to keep the discount, and a wallet dropping below its tier keeps the cached discount only until the entry expires (fee leakage bounded to one 300s window). This is a deliberate gas optimization ([#251](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/251)) — the once-per-window refresh requirement is deemed sufficient CL8Y-sink demand vs the per-swap registry-query gas saved ([GitLab #275](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/275)) | [pair `discount_cache.rs`](../smartcontracts/contracts/pair/src/discount_cache.rs); [`DISCOUNT_CACHE_TTL_SECONDS`](../smartcontracts/packages/dex-common/src/pair.rs) | `fee_discount_coverage_tests`; pair `discount_cache` cache-window tests |
| F1  | **One `CreatePair` flow per block** — at most one pending/instantiate path may start per `env.block.height`; `PAIR_CREATION_BLOCK` gate. **Note:** Cosmos tx atomicity already prevents cross-tx interleaving on `PENDING_PAIR`; this is an explicit rate limit / invariant ([`security-model.md`](./security-model.md#createpair-rate-limit-and-pending-state), GitLab [#121](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/121)) | [factory `PAIR_CREATION_BLOCK`](../smartcontracts/contracts/factory/src/state.rs); [`execute_create_pair`](../smartcontracts/contracts/factory/src/contract.rs) | `tests::test_create_pair_one_per_block_then_next_block_ok` |
| F2  | **Pair-creation fee to treasury** — when `pair_creation_fee_uluna > 0`, `CreatePair` requires attached **uluna** ≥ fee, rejects stray denoms, `BankMsg::Send`s fee to treasury, refunds overpay; fee is atomic with pair instantiate (instantiate failure reverts fee). Governance sets fee via `SetPairCreationFee`. Default **100 LUNC** on new/migrated configs ([#276](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/276)). Bank-send review ([#313](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/313)): [`docs/audits/factory-treasury-bank-send.md`](./audits/factory-treasury-bank-send.md) | [`execute_create_pair`](../smartcontracts/contracts/factory/src/contract.rs); [`Config::pair_creation_fee_uluna`](../smartcontracts/contracts/factory/src/state.rs); [`DEFAULT_PAIR_CREATION_FEE_ULUNA`](../smartcontracts/packages/dex-common/src/factory.rs) | `factory_tests::create_pair_charges_fee_to_treasury_and_gov_can_set_it`, `create_pair_fee_bank_send_adversarial_paths`, `create_pair_refunds_uluna_when_fee_disabled`; `make verify-issue-276`; `make verify-issue-313` |
| R1  | **Router swap state** — no concurrent swap; state cleared after success/failure                                                                     | `SWAP_STATE`                                                                                           | `router_coverage_tests::test_router_failed_swap_then_retry_succeeds`; `reentrancy_tests`; `adversarial_token::router_two_sequential_swaps_both_succeed_state_cleared`                                                |
| R2  | **Deadline** — user deadlines enforced on pair and router                                                                                           | `assert_deadline`                                                                                      | `deadline_tests::test_swap_deadline_exceeded_rejected`, `test_provide_liquidity_deadline_exceeded_rejected`, `test_swap_deadline_not_exceeded_succeeds`, `test_router_deadline_exceeded_rejected`                                                                 |
| R3  | **minimum_receive** — enforced on final output                                                                                                      | Router reply path                                                                                      | `router_coverage_tests::test_router_minimum_receive_assertion`, unwrap variants in [lib.rs](../smartcontracts/tests/src/lib.rs)                      |
| C4  | **Per-hop `min_return`** — pair rejects when net payout (after hook fees) is below `min_return`; router forwards per-hop `min_return` on each `TerraSwap` leg — multi-hop and hybrid paths cannot bypass ([#404](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/404), checklist **SEC-C04**) | [pair `execute_swap`](../smartcontracts/contracts/pair/src/contract.rs); [router hop dispatch](../smartcontracts/contracts/router/src/contract.rs) | `adversarial_token::tax_hook_min_return_checked_against_net_payout` (direct pair + hook fees); `router_coverage_tests::test_router_multi_hop_min_return_rejected_on_second_hop`; `limit_order_tests::hybrid_swap_rejects_min_return_above_net_output` |
| R4  | **Hop output delta** — each multi-hop leg forwards `balance_after − pre_hop_balance` on the hop's output token; pre-existing router dust/donations are **not** swept into the swap chain ([GitLab **#240**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/240), gap **H8**) | [router `reply_swap_hop`](../smartcontracts/contracts/router/src/contract.rs); `SwapState.pre_hop_balance` | `adversarial_token::router_ignores_pre_existing_dust_on_output_token`, `adversarial_token::router_multi_hop_ignores_dust_on_intermediate_and_final_tokens`; [`skills/AGENTS_ROUTER_HOP_ACCOUNTING.md`](../skills/AGENTS_ROUTER_HOP_ACCOUNTING.md) |
| H1  | **Hook caller allowlist** — only allowlisted addresses can invoke `Hook`; **griefing:** allowlisted hook `Err` rolls back the whole swap            | `assert_allowed_pair` in each hook                                                                     | **Allowlist:** `new_feature_tests::swap_with_reverting_hook_fails` (misnamed: disallowed caller → `Unauthorized hook caller`); `hooks_integration_tests::test_hook_unauthorized_caller_rejected`, `test_tax_hook_unauthorized_hook_caller_rejected`, `test_lp_burn_hook_unauthorized_hook_caller_rejected`. **Allowlisted + failing:** `audit_invariant_tests::swap_fails_atomically_when_allowlisted_hook_reverts` |
| H2  | **LP burn hook + forged `pair`** — `AfterSwap.pair` must equal `info.sender`; hook queries pair `liquidity_token` vs config. Non-pair allowlist entries rejected at registration and spoofed payloads fail at execute | [lp-burn-hook `execute_after_swap`](../smartcontracts/contracts/hooks/lp-burn-hook/src/contract.rs) | `adversarial_token::lp_burn_hook_rejects_spoofed_pair_when_spoofer_allowlisted` |
| I2  | **Tax/burn hook settlement** — pair forwards fee slice from ask-token swap output during settlement; no silent treasury subsidy from hook balance | [pair `execute_swap`](../smartcontracts/contracts/pair/src/contract.rs); [hook_settlement](../smartcontracts/packages/dex-common/src/hook_settlement.rs) | `adversarial_token::tax_hook_collects_from_swap_flow_with_zero_treasury_balance`; hook integration tests |
| W1  | **Treasury collateralization** — native backing ≥ CW20 wrapped supply (wrap-mapper / treasury harness)                                              | External `treasury` / `wrap-mapper`                                                                    | `wrap_security_tests::test_wrap_mapper_reentrancy` (invariant check); `wrap_security_tests::test_unwrap_exceeds_treasury_balance`; `wrap_fuzz_tests::prop_wrap_unwrap_treasury_invariant`; [NATIVE_TOKEN_WRAPPING.md](../NATIVE_TOKEN_WRAPPING.md) |
| L1  | **Escrow vs reserves** — `PENDING_ESCROW_TOKEN0/1` tracks maker escrow **for all funds owed to makers**: resting `ORDERS`, plus **`EXPIRED_LIMIT_CLAIMS`** rows until `ClaimExpiredLimitOrder`. Sweep uses `balance − reserves − pending_escrow` ([pair `execute_sweep`](../smartcontracts/contracts/pair/src/contract.rs)). Escrow decrements use **checked** math (no silent underflow). **`expires_at`**: when a hybrid match walk reaches an expired order, it is unlinked into `EXPIRED_LIMIT_CLAIMS` with **`PENDING_ESCROW_*` unchanged**; the maker later receives CW20 via **`ClaimExpiredLimitOrder`** (mirror cancel accounting). The taker tx emits **`limit_order_expired_parked`** and **does not** CW20-transfer to the maker (prevents pending decrement without transfer — GitLab [**#120**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/120)). **`CancelLimitOrder`** only applies to rows still in `ORDERS`, so it cannot double-pay after a park | [state `PENDING_ESCROW_*`, `EXPIRED_LIMIT_CLAIMS`](../smartcontracts/contracts/pair/src/state.rs); [orderbook park + match](../smartcontracts/contracts/pair/src/orderbook.rs); [`execute_claim_expired_limit_order`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests::expired_bid_parked_on_hybrid_walk_claim_refunds_maker`; `orderbook::tests::park_expired_bid_unlinks_and_records_claim_without_pending_delta`; `limit_order_tests::claim_expired_limit_order_non_owner_rejected` (non-owner claim guard); `orderbook::proptest_limits` |
| L2  | **Side-correct CW20 on place** — bids only via token1 CW20 `Send`; asks only via token0 CW20 `Send` | [pair `execute_place_limit_order`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests::place_limit_order_wrong_escrow_token_rejected` |
| L3  | **Cancel owner-only** — only `owner` may `CancelLimitOrder` | [pair `execute_cancel_limit_order`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests::cancel_limit_order_non_owner_rejected` |
| L4  | **Hybrid params** — `pool_input + book_input == amount`; if `book_input > 0` then `max_maker_fills > 0` | [pair `execute_swap`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests` (split mismatch, zero max makers) |
| L5  | **Bounded work** — insert walks capped by `max_adjust_steps` (min with hard cap); valid **`hint_after` / `hint_after_order_id`** verified in O(1) (≤ 2 order loads + head check); near-miss hints on a valid anchor walk **`prev`/`next` from the hint** ([#265](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/265)); missing/wrong-side/unlinked hints use head walk ([#256](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/256)); match uses `max_maker_fills` (min with hard cap **100**, [#262](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/262)); **book walk scan steps capped at `MAX_SCAN_STEPS` (500)** — every list iteration counts, decoupled from maker cap ([#254](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/254), [#262](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/262)); **expired limit parks capped at `MAX_EXPIRED_PARKS_PER_SWAP` (15) per book walk** — additional expired head orders skipped without `EXPIRED_LIMIT_CLAIMS` write ([#250](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/250), benchmarked [#309](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/309)); invalid `book_start_hint` falls back to book head. **Head-only** walks can still hit the scan cap on deep expired prefixes (low griefing — [#289](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/289)); mitigated by **L17** `book_start_hint` and **L15** resumable clean ([#274](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/274)) | [orderbook](../smartcontracts/contracts/pair/src/orderbook.rs) | `limit_order_tests` (steps exceeded, hint fallback, **`hybrid_walk_parks_at_most_max_expired_bids_per_swap`**, **`expired_parks_benchmark_event_counts_sweep`**, ask-side cap, **`hybrid_walk_scan_steps_cap_bounds_expired_prefix_and_spills_to_pool`**, skipped cancel, sim parity); `orderbook::tests::insert_*_hint_*`, `relink_limit_order_price_uses_hint`, `insert_bid_deep_book_near_miss_hint_beats_head_walk_cap`; `orderbook::expired_park_cap_tests::*`; pair `orderbook::proptest_limits::prop_match_bids_maker_cap` |
| L17 | **Match `book_start_hint` side validation** — hybrid `book_start_hint` is advisory: accepted only when the hinted order exists on the **same side** as the active matcher (`match_bids` / `simulate_match_bids` → bid; `match_asks` / `simulate_match_asks` → ask). Wrong-side, stale, or missing hints fall back to the correct head without error. During the walk, orders whose `side` does not match the matcher are **skipped** (no fill, no cross-pool `PENDING_ESCROW_*` debit). Prevents cross-escrow drain when a taker passes an opposite-side order id ([#272](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/272)). **Operational mitigation for expired head-clogs ([#289](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/289)):** integrators supply a live-side hint **past** an expired prefix so the matcher does not exhaust **`MAX_SCAN_STEPS`** at the head. Complements insert hint rules **L14** / **L5** | [`orderbook.rs`](../smartcontracts/contracts/pair/src/orderbook.rs) `resolve_match_start_hint`, `order_on_match_side`, `match_bids`, `match_asks`, `simulate_match_*` | `orderbook::book_start_hint_side_tests::*`; `limit_order_tests::hybrid_wrong_side_book_start_hint_*`, `hybrid_same_side_book_start_hint_still_matches`; `orderbook::proptest_limits::prop_match_bids_adversarial_wrong_side_hint_preserves_escrow`; [limit-orders.md § Expiry](./limit-orders.md#expiry-expires_at); [skills/AGENTS_BOOK_MATCH_HINT_SECURITY.md](../skills/AGENTS_BOOK_MATCH_HINT_SECURITY.md) |
| L6  | **Pause freezes trading and maker withdrawals** — while paused: no `Receive` (swap / place limit), no **`CancelLimitOrder`**, no **`ClaimExpiredLimitOrder`**, and no **`CleanLimitBook`** (including force-clean — global emergency response). Parked expired escrow stays in `EXPIRED_LIMIT_CLAIMS` until unpause. Same `assert_not_paused` gate as cancel so emergency pause can freeze **all** CW20 refund paths that debit pair custody. Active resting orders cannot cancel until unpause | [pair `execute`](../smartcontracts/contracts/pair/src/contract.rs) `assert_not_paused` on `Receive`, `ProvideLiquidity`, `CancelLimitOrder`, **`ClaimExpiredLimitOrder`**, **`CleanLimitBook`**, and `UpdateLimitOrderPrice`; **`IsPaused`** query | `limit_order_tests::pause_blocks_swap_and_place_cancel_refunds_escrow`, `limit_order_tests::claim_expired_limit_order_blocked_while_pair_paused_then_succeeds_after_unpause`, **`clean_limit_book_blocked_while_pair_paused`**; frontend pause banner — [`TradePage.test.tsx`](../frontend-dapp/src/pages/TradePage.test.tsx) ([GitLab **#87**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/87) / [**#263**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/263)); swap + pool disabled CTAs — [`SwapPage.test.tsx`](../frontend-dapp/src/pages/SwapPage.test.tsx), [`PoolPage.test.tsx`](../frontend-dapp/src/pages/PoolPage.test.tsx) ([GitLab **#395**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/395)) |
| L7  | **Hooks + hybrid** — `AfterSwap.commission_amount` is **total protocol commission** in the ask asset (pool treasury transfer + book taker fees); `spread_amount` is **pool leg only**; `return_asset.amount` is **book + pool** net to user. Swap wasm attrs: pool-only `commission_amount` + optional `book_commission_amount` for Terraport baseline ([#196](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/196)) | [pair `execute_swap`](../smartcontracts/contracts/pair/src/contract.rs); [hooks README](../smartcontracts/contracts/hooks/README.md) | `limit_order_tests::hybrid_hook_commission_includes_pool_and_book`, `limit_order_tests::pool_only_hook_commission_unchanged`; [integrators.md](./integrators.md); [skills/AGENTS_HOOK_COMMISSION.md](../skills/AGENTS_HOOK_COMMISSION.md) |
| L10 | **Hybrid swap CW20 aggregation** — book match accumulates `maker_payouts` (offer token, deduped by owner) plus `return_net` / `commission_total` (ask token); `execute_swap` emits **≤ M + 2** CW20 `Transfer` submessages (M distinct makers + one taker + one treasury on ask token). Per-fill `limit_order_fill` events add **`swap_index`** (parent swap ordinal within the tx; [#331](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/331)); hook `commission_amount` and swap attrs unchanged in **value** ([#248](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/248)) | [orderbook `match_bids` / `match_asks`](../smartcontracts/contracts/pair/src/orderbook.rs); [pair `execute_swap`](../smartcontracts/contracts/pair/src/contract.rs); [`tx_swap_index`](../smartcontracts/contracts/pair/src/tx_swap_index.rs) | `orderbook::aggregation_tests::*`; `limit_order_tests::hybrid_swap_emits_limit_order_fill_events`, `hybrid_swap_two_makers_emits_two_fill_events`; [limit-orders.md § Execution order](./limit-orders.md#execution-order-in-execute_swap); [skills/AGENTS_TERRACLASSIC_GAS.md](../skills/AGENTS_TERRACLASSIC_GAS.md) |
| L11 | **Batch cancel / claim refund aggregation** — `CancelLimitOrders` / `ClaimExpiredLimitOrders` unlink or claim up to `max_batch_rungs` ids (hard cap **100**) in one tx; **all-or-nothing** owner checks; refunds sum per token side then emit **≤ 2** CW20 transfers; escrow decrements use checked math once per token (extends **L1**). Pause gate same as single cancel/claim (**L6**) ([#246](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/246)) | [`limit_batch_withdraw.rs`](../smartcontracts/contracts/pair/src/limit_batch_withdraw.rs); [pair `execute`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests::batch_cancel_*`, `batch_claim_expired_two_orders_one_tx`, `batch_claim_expired_foreign_owner_reverts_whole_tx`; indexer `parse_limit_order_cancellations_batch_columnar_attrs`; [limit-orders.md § Batch cancel / claim](./limit-orders.md#batch-cancel-claim-gas-savings) |
| L12 | **Batch placement storage collapse** — `execute_place_limit_orders_batch` reserves all rung ids with one `ORDER_NEXT_ID` write and one `PENDING_ESCROW_*` write per token side touched; final escrow equals sum of placed rung remainings only (skipped rungs still consume ids). Inserts traverse rungs in **book-sort order** (composite key) while **id assignment and wasm attrs stay in input order** ([#266](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/266)). **`UpdateLimitOrderPrice`** relinks without changing `PENDING_ESCROW_*` ([#247](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/247)) | [`limit_placement.rs`](../smartcontracts/contracts/pair/src/limit_placement.rs); [`orderbook.rs`](../smartcontracts/contracts/pair/src/orderbook.rs) `reserve_order_id_block`, `insert_*_with_id_for_batch`, `InsertThreadCursor`; [pair `execute_update_limit_order_price`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests::batch_placement_order_ids_match_sequential_singles`, `limit_batch_bid_ladder_book_order_traversal_places_all_on_empty_book`, `limit_batch_equal_price_fifo_by_order_id`; `orderbook::prop_escrow_dll_after_random_inserts`; [limit-orders.md § Batch placement storage](./limit-orders.md#place--cancel-limit-gitlab-206); [skills/AGENTS_LIMIT_ORDER_BATCH_LADDER.md](../skills/AGENTS_LIMIT_ORDER_BATCH_LADDER.md) §9–11 |
| L13 | **Book match pending-escrow batching** — `match_bids` / `match_asks` accumulate fill escrow deltas in memory and apply **one** `PENDING_ESCROW_TOKEN1` or `PENDING_ESCROW_TOKEN0` load/subtract/save per side per invocation (final escrow equals sequential per-fill subtracts; `checked_sub` underflow unchanged). `simulate_match_*` unchanged. Complements CW20 aggregation **L10** ([#255](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/255)) | [`orderbook.rs`](../smartcontracts/contracts/pair/src/orderbook.rs) `match_bids`, `match_asks`, `escrow_sub_pending_token*` | `orderbook::aggregation_tests::match_bids_batches_pending_escrow_token1_subtract`, `match_asks_batches_pending_escrow_token0_subtract`, `match_bids_pending_escrow_underflow_on_excessive_batch_subtract`; `orderbook::proptest_limits::prop_match_*`; [limit-orders.md § Execution order](./limit-orders.md#execution-order-in-execute_swap); [skills/AGENTS_TERRACLASSIC_GAS.md](../skills/AGENTS_TERRACLASSIC_GAS.md) |
| L15 | **Permissionless limit book clean** — `CleanLimitBook` parks time-expired and/or governance dust orders (per-side `min_remaining_token0/1`; **0** disables force-clean on that side) into `EXPIRED_LIMIT_CLAIMS` with **no** CW20 in the clean tx (**L1**); `max_orders` ≤ **`MAX_LIMIT_CLEAN_ORDERS_HARD_CAP` (100)**; optional **`start_hint`** else head walk; optional **`max_steps`** (absent/`0` → **`MAX_CLEAN_SCAN_STEPS` (500)**) with **`resume_cursor`** when traversal caps — unbounded head walks prohibited ([#274](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/274)); complements taker **`book_start_hint`** for head-clogs ([#289](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/289)). Factory-only `UpdateLimitCleanConfig` / `SetPairLimitCleanConfig`. **Pause-gated** (**L6**). Distinct from factory **`Sweep`** (excess CW20 recovery) ([#263](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/263)) | [`limit_book_clean.rs`](../smartcontracts/contracts/pair/src/limit_book_clean.rs); [pair `execute`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests::clean_limit_book_*` (incl. **`clean_limit_book_scan_cap_resume_reaches_tail`**, `clean_limit_book_emits_no_cw20_transfers`), `update_limit_clean_config_unauthorized`; [limit-orders.md § Permissionless limit book clean](./limit-orders.md#permissionless-limit-book-clean); [skills/AGENTS_LOCALNET_TRADING_SWARM.md](../skills/AGENTS_LOCALNET_TRADING_SWARM.md) |
| L16 | **Match-time dust flush** — after a successful fill in `match_bids` / `match_asks`, when **`0 < remaining < LIMIT_ORDER_DUST_FLUSH_THRESHOLD` (10)** raw units of the side escrow token (token1 bids, token0 asks), the pair **parks** the row via `park_limit_order_for_clean(..., force_expired=true)` — **no** `ORDERS` save, **no** extra pending decrement at park (same **L1** path as time-expiry / **`CleanLimitBook`** parks); fill batched subtract already released **`cost`**; dust **`remaining`** stays in **`PENDING_ESCROW_*`** until **`ClaimExpiredLimitOrder`**. Does **not** consume **`MAX_EXPIRED_PARKS_PER_SWAP`**. **`remaining ≥ 10`** partial saves unchanged. **`simulate_match_*`** treats flushed dust as gone in-memory (**L8**). Complements governance **`CleanLimitBook`** (**L15**) ([#264](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/264)) | [`orderbook.rs`](../smartcontracts/contracts/pair/src/orderbook.rs) `should_flush_dust`, `finalize_order_after_fill`, `match_bids`, `match_asks`, `simulate_match_*`; [`dex-common::pair`](../smartcontracts/packages/dex-common/src/pair.rs) `LIMIT_ORDER_DUST_FLUSH_THRESHOLD` | `orderbook::aggregation_tests::match_bid_dust_*`, `match_asks_dust_*`, `match_bids_multi_maker_dust_flush_sim_matches_execute`; `limit_order_tests::match_dust_flush_bid_hybrid_then_maker_claims`; [limit-orders.md § Match-time dust flush](./limit-orders.md#match-time-dust-flush-gitlab-264); [skills/AGENTS_FRONTEND_LIMIT_PARKED_EXPIRED.md](../skills/AGENTS_FRONTEND_LIMIT_PARKED_EXPIRED.md) |
| L14 | **Insert hint verification** — `hint_after` / `hint_after_order_id` is advisory: O(1) verify (hint + optional `next`, plus head check when `prev` is null) then insert; when O(1) fails but the hint is a **valid anchor** (exists, correct side, linked), walk **`prev` toward head** if the new order sorts before the hint, else **`next` toward tail** from `hint.next` (or `hint` when `next` is missing/wrong-side) — shared `steps` budget, single strategy per insert (no chained head walk) ([#265](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/265)); stale / wrong-side / unlinked hints fall back to bounded **head** walk ([#256](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/256)). Batch/ladder: per-rung **`hint_after_order_id`** when set; else **`InsertThreadCursor`** from the prior successful book-order insert (O(0)-load verify when bracketed) or prior rung id as hint ([#266](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/266)); ladder **`LimitOrderLadderSpec.hint_after_order_id`** applies to the head-most rung only (anchor via indexer **#267**). | [`orderbook.rs`](../smartcontracts/contracts/pair/src/orderbook.rs) `try_insert_after_hint_*`, `try_insert_after_thread_cursor_*`, `walk_insert_*`, `find_insert_*`; [`limit_placement.rs`](../smartcontracts/contracts/pair/src/limit_placement.rs); [`execute_update_limit_order_price`](../smartcontracts/contracts/pair/src/contract.rs) | `orderbook::tests::insert_*_hint_*`, `insert_bid_deep_book_near_miss_hint_beats_head_walk_cap`, `relink_limit_order_price_uses_hint`, `limit_batch_item_explicit_hint_places_on_deep_book`, `limit_batch_ladder_anchor_hint_places_boundary_on_deep_book`, `limit_batch_bid_ladder_book_order_traversal_places_all_on_empty_book`; [limit-orders.md § hint_after](./limit-orders.md#place--cancel-limit-gitlab-206); [integrators.md § Batch placement insert hints](./integrators.md#batch-placement-insert-hints-gitlab-261); [skills/AGENTS_LIMIT_ORDER_BATCH_LADDER.md](../skills/AGENTS_LIMIT_ORDER_BATCH_LADDER.md) |
| L9  | **Unified `max_spread`** — pool-only and hybrid swaps share one check in [`dex_common::max_spread`](../smartcontracts/packages/dex-common/src/max_spread.rs): **no `belief_price`** — `(min(pool_spread, pool_gross) + book_shortfall) / (pool_gross + book_net)` where `book_shortfall = max(0, pool_net × book_input / pool_input − book_net)` when both legs are present ([#273](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/273)); **pure-book** (`pool_input = 0`) is not bounded by this metric — on **execute** require `belief_price` or pair `min_return` ([#334](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/334)); router book hops set per-hop `min_return`. When **`declared_pool_input > 0`** and `book_input > 0`, require **`declared_pool_input ≥ 10%` of `offer_amount`** (min 1 raw unit) and **`pool_net > 0`** ([#307](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/307)); pair/router reject early on declared split. Pure-book (`declared_pool_input = 0`) may still roll unfilled book remainder to the pool without triggering #307. **`belief_price`** — shortfall vs `offer / belief_price` using `book_net + pool_net + pool_commission` as actual output ([#197](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/197), [integrators.md](./integrators.md#slippage-max_spread-and-belief_price-hybrid)) | [pair `assert_max_spread`](../smartcontracts/contracts/pair/src/contract.rs); [router hybrid split precheck](../smartcontracts/contracts/router/src/contract.rs); [ADR 0001](./adr/0001-hybrid-quoting-and-routing.md) | `dex-common` `max_spread` unit tests (incl. #273 book shortfall, #307 material pool); `limit_order_tests::hybrid_pure_book_requires_slippage_floor_without_belief`, `hybrid_no_belief_book_far_below_pool_rejected`, `hybrid_no_belief_dust_pool_leg_rejected`, `hybrid_max_spread_exact_tolerance_succeeds`, `hybrid_max_spread_tighter_than_simulation_rejected`, `hybrid_belief_price_max_spread_rejects_shortfall_on_total_output`; pool-only regression `test_swap_max_spread` in [lib.rs](../smartcontracts/tests/src/lib.rs); frontend [`swapMaxSpread.ts`](../frontend-dapp/src/utils/swapMaxSpread.ts) preflight |
| L8  | **Quotes:** Legacy pair `Simulation` / `ReverseSimulation` were **removed** ([GitLab #190](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/190)). All quotes use **`HybridSimulation` / `HybridReverseSimulation`**. Pool-only: `dex_common::pair::pool_only_hybrid_params(offer)` (forward) or `pool_only_hybrid_template()` (reverse ratio). **`HybridSimulation` / `HybridReverseSimulation` optional `trader` / `sender`:** when omitted, quotes use full pair `fee_bps` (backward compatible); when `trader` is set, CL8Y fee-tier discount matches execute ([GitLab **#238**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/238)). **Off-chain callers** (dapp preflight, indexer route solve) must forward the connected wallet as `trader` on every LCD hop when quoting for a registered tier holder ([GitLab **#245**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/245)). Sim queries are read-only (no deregister side-effects). **Bounded query gas:** the discount registry is queried **at most once per quote** — forward sim resolves the effective fee then calls the core `simulate_hybrid_swap_with_fee`; `HybridReverseSimulation` resolves it **once before** its search and reuses it across all iterations (never per-iteration), so reverse quotes for discounted traders stay within the LCD smart-query gas cap (#238 guardrail). **Reverse search ([GitLab #257](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/257)):** upper bound seeded from pool constant-product reverse math; at most **`MAX_HYBRID_REVERSE_SIM_CALLS` (32)** full `simulate_hybrid_swap_with_fee` calls per query (minimal-offer invariant unchanged). On-chain check: [`scripts/qa/verify-issue-238.sh`](../scripts/qa/verify-issue-238.sh) (`make verify-issue-238`). Router `SimulateSwapOperations` forwards the same optional fields per hop. With non-zero `book_input`, quotes walk the limit book read-only (see `docs/limit-orders.md`); parity vs execute assumes the same chain snapshot (queries do not mutate expired head cleanup). Indexer **`GET /api/v1/route/solve`** (hybrid-aware **by default** when `amount_in` is set — [GitLab **#191**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/191); legacy **`pool_only=true`**) and **`POST`** (`hybrid_by_hop`) merge hybrid into router ops and LCD `simulate_swap_operations` when `ROUTER_ADDRESS` is set; optional query/body **`trader`** / **`sender`** forwarded to all pair-level `HybridSimulation` calls and router sim (**#245**). **Multihop:** per-hop `pool_input + book_input` must equal that hop’s simulated offer amount; 3+ hop paths with hybrid on multiple legs: [GitLab **#192**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/192). | [pair `simulate_hybrid_swap`](../smartcontracts/contracts/pair/src/contract.rs); [pair `hybrid_reverse`](../smartcontracts/contracts/pair/src/hybrid_reverse.rs) (#257); [router sim](../smartcontracts/contracts/router/src/contract.rs); [indexer `route_solver`](../indexer/src/api/route_solver.rs); [ADR 0001](./adr/0001-hybrid-quoting-and-routing.md) | `limit_order_tests::router_simulate_swap_hybrid_matches_pool_when_book_empty`, `hybrid_forward_sim_matches_execute_when_book_empty`, `hybrid_reverse_pool_only_template_is_stable`, **`hybrid_reverse_sim_minimal_offer_invariant`**, `hybrid_pool_and_book_legs_one_swap`, `router_two_hop_first_leg_hybrid_matches_simulate`, **`router_three_hop_two_legs_hybrid_matches_simulate`**, **`hybrid_simulation_matches_execute_with_fee_discount`**; indexer `api_route_solve` (incl. `route_solve_post_three_hop_multi_leg_hybrid`, `route_solve_get_hybrid_optimize_three_hops`, **`route_solve_get_default_hybrid_two_hops`**, **`route_solve_get_with_trader_returns_higher_estimate`**); [limit-orders.md](./limit-orders.md); [skills/AGENTS_HYBRID_QUOTING.md](../skills/AGENTS_HYBRID_QUOTING.md); [`skills/AGENTS_TESTING_MULTIHOP_HYBRID.md`](../skills/AGENTS_TESTING_MULTIHOP_HYBRID.md) |
| B1  | **Trading blacklist gates user executes** — factory `BlacklistCheck` before pair/router user paths; wallet blacklist blocks swap (pool and hybrid), LP provide/withdraw, limit place/cancel/**`ClaimExpiredLimitOrder`**/**`UpdateLimitOrderPrice`**, and router multihop; token/pair dimensions block trades on affected assets/contracts. **`Unblacklist*`** restores service; escrow/LP balances unchanged ([ADR 0003](./adr/0003-governance-trading-blacklist.md), [#308](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/308)) | [pair `gate_trading_blacklist`](../smartcontracts/contracts/pair/src/contract.rs); [pair `blacklist_guard.rs`](../smartcontracts/contracts/pair/src/blacklist_guard.rs); [router `blacklist_guard.rs`](../smartcontracts/contracts/router/src/blacklist_guard.rs) | `blacklist_tests::wallet_blacklist_blocks_swap_lp_limits_and_unban_restores` (SEC-B02 / [#393](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/393)); `router_multihop_rejects_blacklisted_wallet`; `token_blacklist_blocks_swap_both_directions`; `pair_blacklist_blocks_swap_and_lp`; [security-model.md § Trading blacklist](./security-model.md#trading-blacklist-compliance--incident-response) |
| C14 | **Migration preserves critical storage** — wasm `Migrate` on factory, pair, and fee-discount must not drop or corrupt governance/treasury config, pair registry, LP-token admin, trading blacklists, discount tiers/registrations/trusted routers, or open limit-order book state. Version downgrade attempts are rejected (`cw2::ensure_from_older_version`). Prior deployed wasm binaries are **not** in-repo; integration tests simulate upgrades by downgrading cw2 metadata in `cw-multi-test` storage then migrating to the current code id ([#405](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/405)). | [`factory::migrate`](../smartcontracts/contracts/factory/src/contract.rs); [`pair::migrate`](../smartcontracts/contracts/pair/src/contract.rs); [`fee-discount::migrate`](../smartcontracts/contracts/fee-discount/src/contract.rs) | `migration_tests::factory_migration_preserves_governance_treasury_registry_blacklists_and_lp_admin`; `migration_tests::pair_migration_preserves_fee_registry_lp_admin_and_limit_book`; `migration_tests::fee_discount_migration_preserves_tiers_registrations_and_trusted_routers`; `migration_tests::pair_migration_checks_version`; [wasm admin runbook § Automated regression](./runbooks/wasm-admin-migration.md#automated-regression-sec-c14) |


## Limit orders and hybrid swaps (pair)

See invariant rows **L1–L10** above and [`limit-orders.md`](./limit-orders.md) for message shapes, pause semantics, indexer hints, and simulation limits.

## Attack paths considered (non-governance)

Historical **external** incidents (Terra IBC-hooks, Osmosis LP-share, Levana oracle timing, malicious CW20, fake pair indexing, SQL injection, LCD amplification, admin key compromise, wallet phishing, stale deploy) are traced by date and protocol in [exploit-replay-matrix.md](./exploit-replay-matrix.md) (SEC-D02–D12, [#406](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/406)). Agent playbook: [`skills/AGENTS_EXPLOIT_REPLAY_MATRIX.md`](../skills/AGENTS_EXPLOIT_REPLAY_MATRIX.md).

- Unauthorized factory/pair/hook/discount admin calls → blocked (`Unauthorized`).
- Discount theft via fake `trader` from non-trusted router → blocked (query uses `sender`).
- Sandwich / flash-style value extraction → mitigated by fees + `max_spread` / slippage tests (`security_tests`, proptest).
- Fee-on-transfer / reserve imbalance → documented; sweep recovers excess balance without editing reserves.
- Hook griefing (reverting hook) → intentional if governance registered an allowlisted hook that errors; tested (`audit_invariant_tests::swap_fails_atomically_when_allowlisted_hook_reverts` — full tx rollback; pool unchanged). Disallowed caller → `new_feature_tests::swap_with_reverting_hook_fails` + `hooks_integration_tests::test_*_unauthorized_hook_caller_rejected` (burn / tax / lp-burn).
- Commission treasury on failed swap → commission transfer is part of atomic swap execution; max_spread and deadline rejections leave treasury unchanged (**P10** / SEC-C03 / [#402](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/402)): `audit_invariant_tests::commission_treasury_unchanged_after_max_spread_rejected_swap`, `commission_treasury_unchanged_after_deadline_rejected_swap`.
- Router `SWAP_STATE` after a failed tx → not stuck; `router_coverage_tests::test_router_failed_swap_then_retry_succeeds`.
- Router dust / attribution → `adversarial_token::router_ignores_pre_existing_dust_on_output_token`, `adversarial_token::router_multi_hop_ignores_dust_on_intermediate_and_final_tokens` (R4 / [#240](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/240)).

## Residual risks (not “bugs” under trusted governance)

- **Malicious governance** can set destructive hooks, pause pairs (blocking swaps, new limit placements, limit cancels, limit price updates, and **parked-expiry claims** until unpause), or point discount registry to broken contracts (users pay full fee if query fails). Parked `EXPIRED_LIMIT_CLAIMS` rows remain claimable only after unpause (GitLab [**#120**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/120)).
- **Wasm admin / migration** on-chain is outside these crates; deployment checklist should restrict migration keys. Automated state-preservation regression: [wasm admin runbook § SEC-C14](./runbooks/wasm-admin-migration.md#automated-regression-sec-c14) ([#405](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/work_items/405)).
- **Indexer / frontend** are not authoritative for on-chain safety; oracle/TWAP consumers must follow disclaimers in `dex-common` pair query docs.

## Third-party audit

[security-model.md](./security-model.md) notes that a formal third-party audit is recommended before high-TVL mainnet. This document does **not** replace an external audit.

## Maintenance

When adding a new execute path or economic rule:

1. Add a row to the invariant matrix (or extend an existing row).
2. Add a **deterministic** regression test that encodes the business rule (not only line coverage).
3. If the property is numeric or sequence-based, add or extend a **proptest** in `cl8y-dex-tests`.
4. Run `cargo test` in `smartcontracts/` and, for coverage, see [testing.md](./testing.md) (Rust / LLVM coverage).

