# CL8Y DEX — Contracts Security Audit (Repository)

This document is the **in-repo security audit and invariant matrix** for the CosmWasm contracts under `smartcontracts/`. It complements [security-model.md](./security-model.md) (operational trust assumptions) and [testing.md](./testing.md) (how to run tests and coverage).

## Trust model (explicit)


| Assumption                  | Implication for findings                                                                                                                                                                                        |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Governance is honest**    | Multisig/DAO-controlled governance is trusted not to sabotage the protocol (malicious hooks, fee changes, pauses) on purpose. The audit still verifies **non-governance callers cannot** perform admin actions. |
| **Treasury is a sink**      | Commission tokens are transferred to `treasury`; misconfiguration of treasury address is an ops risk, not an unauthorized-access bug.                                                                           |
| **CW20 code ID whitelist**  | Only whitelisted token **code IDs** may back pairs; malicious token *logic* inside a whitelisted template remains a product risk (e.g. fee-on-transfer), mitigated by docs and tests.                           |
| **Hooks are opt-in policy** | Governance may register hooks that **revert**; by design that blocks swaps ([security-model.md](./security-model.md)).                                                                                          |
| **External git deps**       | `treasury`, `wrap-mapper`, `cw20-mintable` (see [smartcontracts/Cargo.toml](../smartcontracts/Cargo.toml)) are part of the trust boundary for wrap/treasury scenarios in tests and any integrated deployment.   |


## Contract inventory


| Crate / path                                       | Role                                                                                                              |
| -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| `cl8y-dex-factory`                                 | Pair creation, whitelist, governance-only pair config (fees, hooks, discount registry, pause, sweep). **Discount registry rollout:** bounded `SetDiscountRegistryBatch` for large pair counts; `SetDiscountRegistryAll` capped at ≤10 pairs ([#242](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/242), [contracts-terraclassic.md § rollout](./contracts-terraclassic.md#factory-discount-registry-rollout-invariants-glab-123), [#123](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/123)).             |
| `cl8y-dex-pair`                                    | Constant-product AMM, reserves, fees, TWAP observations, post-swap hooks, pause/sweep, **FIFO limit book** + Pattern C hybrid swaps. |
| `cl8y-dex-router`                                  | Multi-hop swaps, `SwapState` + reply chain, `trader` forwarding for discounts, optional unwrap via `wrap_mapper`. |
| `cl8y-dex-fee-discount`                            | Tiered discounts, EOA registration, trusted routers, lazy deregistration.                                         |
| `cl8y-dex-burn-hook` / `tax-hook` / `lp-burn-hook` | Post-swap callbacks; gated by **allowed caller** (expected: real pair contracts).                                 |
| `dex-common`                                       | Shared types, oracle math, hook wire format.                                                                      |


## Invariant matrix

Each row states a property that should **always** hold (under the trust model). **Test** column points to the primary automated evidence in `smartcontracts/tests/`.


| ID  | Invariant                                                                                                                                           | Code / notes                                                                                           | Tests / fuzz                                                                                                                                         |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1  | **k monotonicity** — after a swap, k' \geq k within documented rounding bounds                                                                      | [pair `execute_swap](../smartcontracts/contracts/pair/src/contract.rs)`                                | `audit_invariant_tests::p1_k_non_decreasing_after_swap`; `fuzz_tests` / `additional_fuzz_tests` / `security_tests` (K checks); [pair `lib.rs`](../smartcontracts/contracts/pair/src/lib.rs) |
| P2  | **Reserves vs balances** — internal `RESERVES` update only via protocol messages; direct CW20 transfers can desync balance vs reserves              | Documented in pair `lib.rs`                                                                            | `adversarial_token::fee_on_transfer_creates_reserve_imbalance`; `sweep_tests` (excess recovery)                                                                                       |
| P3  | **First-deposit LP lock** — `MINIMUM_LIQUIDITY` burned on first mint; **bootstrap decimals gate** (`MAX_PAIR_ASSET_DECIMALS_BOOTSTRAP` = **18**) on factory `CreatePair` and empty-pool `provide_liquidity` rejects asset CW20s with **`decimals > 18`** ([#124](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/124)) | [pair `execute_provide_liquidity`](../smartcontracts/contracts/pair/src/contract.rs); [factory `create_pair`](../smartcontracts/contracts/factory/src/contract.rs); LP CW20 `decimals` = **`LP_TOKEN_DECIMALS` (18)** (`dex-common`) | `pair_coverage_tests::test_first_deposit_minimum_liquidity`, `test_first_deposit_below_minimum_rejected`, `test_lp_token_instantiated_with_lp_token_decimals_constant`, `test_create_pair_rejects_cw20_above_bootstrap_decimal_cap`, `test_empty_pool_provide_rejects_asset_decimals_above_bootstrap_cap`; `security_tests::test_lp_share_inflation_first_depositor_griefing` |
| P4  | **Fee bounds** — `fee_bps` / effective fee in valid range; commission rounding sane. **Ask-side book fills:** commission on limit matches is taken on the **token0** output leg (not token1 `cost`)                                                                 | Pair fee + discount composition; [orderbook `match_asks`](../smartcontracts/contracts/pair/src/orderbook.rs)                                                                        | `fee_discount_coverage_tests`, integration swap tests, `fee_math_property_tests` (proptest); `limit_order_tests::hybrid_ask_non_unity_price_treasury_fee_in_token0`                                                          |
| P5  | **Discount registry failure → full fee** — if `GetDiscount` query fails, pair uses configured pair fee                                              | [pair `execute_swap` `Err(_) => fee_config.fee_bps](../smartcontracts/contracts/pair/src/contract.rs)` | `audit_invariant_tests::swap_uses_full_fee_when_discount_registry_query_fails`                                                                       |
| P6  | **Untrusted router cannot steal discount** — `trader` only applies when sender is trusted router                                                    | [fee-discount `GetDiscount](../smartcontracts/contracts/fee-discount/src/contract.rs)`                 | `fee_discount_coverage_tests::test_query_discount_untrusted_router_falls_back_to_sender`                                                             |
| P7  | **Factory-only pair admin** — fee, hooks, registry, pause, sweep on pair only from factory                                                          | Pair `execute_*` sender checks                                                                         | `audit_invariant_tests::p7_non_factory_cannot_update_pair_fee`; `line_coverage_tests`, `pair_coverage_tests`, `factory_coverage_tests`                                                                              |
| P8  | **Governance-only factory admin**                                                                                                                   | [factory `ensure_governance](../smartcontracts/contracts/factory/src/contract.rs)`                     | `audit_invariant_tests::p8_non_governance_cannot_add_whitelisted_code_id`; `factory_coverage_tests` (unauthorized)                                                                                                   |
| F1  | **One `CreatePair` flow per block** — at most one pending/instantiate path may start per `env.block.height`; `PAIR_CREATION_BLOCK` gate. **Note:** Cosmos tx atomicity already prevents cross-tx interleaving on `PENDING_PAIR`; this is an explicit rate limit / invariant ([`security-model.md`](./security-model.md#createpair-rate-limit-and-pending-state), GitLab [#121](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/121)) | [factory `PAIR_CREATION_BLOCK`](../smartcontracts/contracts/factory/src/state.rs); [`execute_create_pair`](../smartcontracts/contracts/factory/src/contract.rs) | `tests::test_create_pair_one_per_block_then_next_block_ok` |
| R1  | **Router swap state** — no concurrent swap; state cleared after success/failure                                                                     | `SWAP_STATE`                                                                                           | `router_coverage_tests::test_router_failed_swap_then_retry_succeeds`; `reentrancy_tests`; `adversarial_token::router_two_sequential_swaps_both_succeed_state_cleared`                                                |
| R2  | **Deadline** — user deadlines enforced on pair and router                                                                                           | `assert_deadline`                                                                                      | `deadline_tests::test_swap_deadline_exceeded_rejected`, `test_provide_liquidity_deadline_exceeded_rejected`, `test_swap_deadline_not_exceeded_succeeds`, `test_router_deadline_exceeded_rejected`                                                                 |
| R3  | **minimum_receive** — enforced on final output                                                                                                      | Router reply path                                                                                      | `router_coverage_tests::test_router_minimum_receive_assertion`, unwrap variants in [lib.rs](../smartcontracts/tests/src/lib.rs)                      |
| R4  | **Hop output delta** — each multi-hop leg forwards `balance_after − pre_hop_balance` on the hop's output token; pre-existing router dust/donations are **not** swept into the swap chain ([GitLab **#240**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/240), gap **H8**) | [router `reply_swap_hop`](../smartcontracts/contracts/router/src/contract.rs); `SwapState.pre_hop_balance` | `adversarial_token::router_ignores_pre_existing_dust_on_output_token`, `adversarial_token::router_multi_hop_ignores_dust_on_intermediate_and_final_tokens`; [`skills/AGENTS_ROUTER_HOP_ACCOUNTING.md`](../skills/AGENTS_ROUTER_HOP_ACCOUNTING.md) |
| H1  | **Hook caller allowlist** — only allowlisted addresses can invoke `Hook`; **griefing:** allowlisted hook `Err` rolls back the whole swap            | `assert_allowed_pair` in each hook                                                                     | **Allowlist:** `new_feature_tests::swap_with_reverting_hook_fails` (misnamed: disallowed caller → `Unauthorized hook caller`); `hooks_integration_tests::test_hook_unauthorized_caller_rejected`, `test_tax_hook_unauthorized_hook_caller_rejected`, `test_lp_burn_hook_unauthorized_hook_caller_rejected`. **Allowlisted + failing:** `audit_invariant_tests::swap_fails_atomically_when_allowlisted_hook_reverts` |
| H2  | **LP burn hook + forged `pair`** — if a **non-pair** address is allowlisted, spoofed `AfterSwap` can drive burns (governance misconfiguration risk) | [lp-burn-hook `execute_after_swap](../smartcontracts/contracts/hooks/lp-burn-hook/src/contract.rs)`    | `adversarial_token::lp_burn_hook_accepts_spoofed_pair_when_spoofer_allowlisted`                                                                      |
| W1  | **Treasury collateralization** — native backing ≥ CW20 wrapped supply (wrap-mapper / treasury harness)                                              | External `treasury` / `wrap-mapper`                                                                    | `wrap_security_tests::test_wrap_mapper_reentrancy` (invariant check); `wrap_security_tests::test_unwrap_exceeds_treasury_balance`; `wrap_fuzz_tests::prop_wrap_unwrap_treasury_invariant`; [NATIVE_TOKEN_WRAPPING.md](../NATIVE_TOKEN_WRAPPING.md) |
| L1  | **Escrow vs reserves** — `PENDING_ESCROW_TOKEN0/1` tracks maker escrow **for all funds owed to makers**: resting `ORDERS`, plus **`EXPIRED_LIMIT_CLAIMS`** rows until `ClaimExpiredLimitOrder`. Sweep uses `balance − reserves − pending_escrow` ([pair `execute_sweep`](../smartcontracts/contracts/pair/src/contract.rs)). Escrow decrements use **checked** math (no silent underflow). **`expires_at`**: when a hybrid match walk reaches an expired order, it is unlinked into `EXPIRED_LIMIT_CLAIMS` with **`PENDING_ESCROW_*` unchanged**; the maker later receives CW20 via **`ClaimExpiredLimitOrder`** (mirror cancel accounting). The taker tx emits **`limit_order_expired_parked`** and **does not** CW20-transfer to the maker (prevents pending decrement without transfer — GitLab [**#120**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/120)). **`CancelLimitOrder`** only applies to rows still in `ORDERS`, so it cannot double-pay after a park | [state `PENDING_ESCROW_*`, `EXPIRED_LIMIT_CLAIMS`](../smartcontracts/contracts/pair/src/state.rs); [orderbook park + match](../smartcontracts/contracts/pair/src/orderbook.rs); [`execute_claim_expired_limit_order`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests::expired_bid_parked_on_hybrid_walk_claim_refunds_maker`; `orderbook::tests::park_expired_bid_unlinks_and_records_claim_without_pending_delta`; `orderbook::proptest_limits` |
| L2  | **Side-correct CW20 on place** — bids only via token1 CW20 `Send`; asks only via token0 CW20 `Send` | [pair `execute_place_limit_order`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests::place_limit_order_wrong_escrow_token_rejected` |
| L3  | **Cancel owner-only** — only `owner` may `CancelLimitOrder` | [pair `execute_cancel_limit_order`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests::cancel_limit_order_non_owner_rejected` |
| L4  | **Hybrid params** — `pool_input + book_input == amount`; if `book_input > 0` then `max_maker_fills > 0` | [pair `execute_swap`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests` (split mismatch, zero max makers) |
| L5  | **Bounded work** — insert walks capped by `max_adjust_steps` (min with hard cap); match uses `max_maker_fills` (min with hard cap); **book walk scan steps capped at `MAX_SCAN_STEPS` (288)** — every list iteration counts ([#254](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/254)); **expired limit parks capped at `MAX_EXPIRED_PARKS_PER_SWAP` (15) per book walk** — additional expired head orders skipped without `EXPIRED_LIMIT_CLAIMS` write ([#250](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/250)); invalid `book_start_hint` falls back to book head | [orderbook](../smartcontracts/contracts/pair/src/orderbook.rs) | `limit_order_tests` (steps exceeded, hint fallback, **`hybrid_walk_parks_at_most_max_expired_bids_per_swap`**, ask-side cap, **`hybrid_walk_scan_steps_cap_bounds_expired_prefix_and_spills_to_pool`**, skipped cancel, sim parity); `orderbook::expired_park_cap_tests::*`; pair `orderbook::proptest_limits::prop_match_bids_maker_cap` |
| L6  | **Pause freezes trading and maker withdrawals** — while paused: no `Receive` (swap / place limit), no **`CancelLimitOrder`**, and no **`ClaimExpiredLimitOrder`** (parked expired escrow stays in `EXPIRED_LIMIT_CLAIMS` until unpause). Same `assert_not_paused` gate as cancel so emergency pause can freeze **all** CW20 refund paths that debit pair custody. Active resting orders cannot cancel until unpause | [pair `execute`](../smartcontracts/contracts/pair/src/contract.rs) `assert_not_paused` on `Receive`, `ProvideLiquidity`, `CancelLimitOrder`, **`ClaimExpiredLimitOrder`**, and `UpdateLimitOrderPrice`; **`IsPaused`** query | `limit_order_tests::pause_blocks_swap_and_place_cancel_refunds_escrow`, `limit_order_tests::claim_expired_limit_order_blocked_while_pair_paused_then_succeeds_after_unpause`; frontend pause banner — [`TradePage.test.tsx`](../frontend-dapp/src/pages/TradePage.test.tsx) ([GitLab **#87**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/87) / epic [**#199**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/199)) |
| L7  | **Hooks + hybrid** — `AfterSwap.commission_amount` is **total protocol commission** in the ask asset (pool treasury transfer + book taker fees); `spread_amount` is **pool leg only**; `return_asset.amount` is **book + pool** net to user. Swap wasm attrs: pool-only `commission_amount` + optional `book_commission_amount` for Terraport baseline ([#196](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/196)) | [pair `execute_swap`](../smartcontracts/contracts/pair/src/contract.rs); [hooks README](../smartcontracts/contracts/hooks/README.md) | `limit_order_tests::hybrid_hook_commission_includes_pool_and_book`, `limit_order_tests::pool_only_hook_commission_unchanged`; [integrators.md](./integrators.md); [skills/AGENTS_HOOK_COMMISSION.md](../skills/AGENTS_HOOK_COMMISSION.md) |
| L10 | **Hybrid swap CW20 aggregation** — book match accumulates `maker_payouts` (offer token, deduped by owner) plus `return_net` / `commission_total` (ask token); `execute_swap` emits **≤ M + 2** CW20 `Transfer` submessages (M distinct makers + one taker + one treasury on ask token). Per-fill `limit_order_fill` events unchanged; hook `commission_amount` and swap attrs unchanged in **value** ([#248](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/248)) | [orderbook `match_bids` / `match_asks`](../smartcontracts/contracts/pair/src/orderbook.rs); [pair `execute_swap`](../smartcontracts/contracts/pair/src/contract.rs) | `orderbook::aggregation_tests::*`; `limit_order_tests::hybrid_swap_two_makers_emits_two_fill_events`, `hybrid_aggregated_maker_payouts_multi_maker`; [limit-orders.md § Execution order](./limit-orders.md#execution-order-in-execute_swap); [skills/AGENTS_TERRACLASSIC_GAS.md](../skills/AGENTS_TERRACLASSIC_GAS.md) |
| L11 | **Batch cancel / claim refund aggregation** — `CancelLimitOrders` / `ClaimExpiredLimitOrders` unlink or claim up to `max_batch_rungs` ids (hard cap 30) in one tx; **all-or-nothing** owner checks; refunds sum per token side then emit **≤ 2** CW20 transfers; escrow decrements use checked math once per token (extends **L1**). Pause gate same as single cancel/claim (**L6**) ([#246](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/246)) | [`limit_batch_withdraw.rs`](../smartcontracts/contracts/pair/src/limit_batch_withdraw.rs); [pair `execute`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests::batch_cancel_*`, `batch_claim_expired_two_orders_one_tx`; indexer `parse_limit_order_cancellations_batch_columnar_attrs`; [limit-orders.md § Batch cancel / claim](./limit-orders.md#batch-cancel-claim-gas-savings) |
| L12 | **Batch placement storage collapse** — `execute_place_limit_orders_batch` reserves all rung ids with one `ORDER_NEXT_ID` write and one `PENDING_ESCROW_*` write per token side touched; final escrow equals sum of placed rung remainings only (skipped rungs still consume ids). **`UpdateLimitOrderPrice`** relinks without changing `PENDING_ESCROW_*` ([#247](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/247)) | [`limit_placement.rs`](../smartcontracts/contracts/pair/src/limit_placement.rs); [`orderbook.rs`](../smartcontracts/contracts/pair/src/orderbook.rs) `reserve_order_id_block`, `insert_*_with_id`; [pair `execute_update_limit_order_price`](../smartcontracts/contracts/pair/src/contract.rs) | `limit_order_tests::batch_placement_order_ids_match_sequential_singles`; `orderbook::prop_escrow_dll_after_random_inserts`; [limit-orders.md § Batch placement storage](./limit-orders.md#place--cancel-limit-gitlab-206); [skills/AGENTS_LIMIT_ORDER_BATCH_LADDER.md](../skills/AGENTS_LIMIT_ORDER_BATCH_LADDER.md) §9 |
| L13 | **Book match pending-escrow batching** — `match_bids` / `match_asks` accumulate fill escrow deltas in memory and apply **one** `PENDING_ESCROW_TOKEN1` or `PENDING_ESCROW_TOKEN0` load/subtract/save per side per invocation (final escrow equals sequential per-fill subtracts; `checked_sub` underflow unchanged). `simulate_match_*` unchanged. Complements CW20 aggregation **L10** ([#255](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/255)) | [`orderbook.rs`](../smartcontracts/contracts/pair/src/orderbook.rs) `match_bids`, `match_asks`, `escrow_sub_pending_token*` | `orderbook::aggregation_tests::match_bids_batches_pending_escrow_token1_subtract`, `match_asks_batches_pending_escrow_token0_subtract`, `match_bids_pending_escrow_underflow_on_excessive_batch_subtract`; `orderbook::proptest_limits::prop_match_*`; [limit-orders.md § Execution order](./limit-orders.md#execution-order-in-execute_swap); [skills/AGENTS_TERRACLASSIC_GAS.md](../skills/AGENTS_TERRACLASSIC_GAS.md) |
| L9  | **Unified `max_spread`** — pool-only and hybrid swaps share one check in [`dex_common::max_spread`](../smartcontracts/packages/dex-common/src/max_spread.rs): **no `belief_price`** — `min(pool_spread, pool_gross) / (pool_gross + book_net)`; **`belief_price`** — shortfall vs `offer / belief_price` using `book_net + pool_net + pool_commission` as actual output ([GitLab **#197**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/197), [integrators.md](./integrators.md#slippage-max_spread-and-belief_price-hybrid)) | [pair `assert_max_spread`](../smartcontracts/contracts/pair/src/contract.rs); [ADR 0001](./adr/0001-hybrid-quoting-and-routing.md) | `dex-common` `max_spread` unit tests; `limit_order_tests::hybrid_max_spread_exact_tolerance_succeeds`, `hybrid_max_spread_tighter_than_simulation_rejected`, `hybrid_belief_price_max_spread_rejects_shortfall_on_total_output`; pool-only regression `test_swap_max_spread` in [lib.rs](../smartcontracts/tests/src/lib.rs) |
| L8  | **Quotes:** Legacy pair `Simulation` / `ReverseSimulation` were **removed** ([GitLab #190](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/190)). All quotes use **`HybridSimulation` / `HybridReverseSimulation`**. Pool-only: `dex_common::pair::pool_only_hybrid_params(offer)` (forward) or `pool_only_hybrid_template()` (reverse ratio). **`HybridSimulation` / `HybridReverseSimulation` optional `trader` / `sender`:** when omitted, quotes use full pair `fee_bps` (backward compatible); when `trader` is set, CL8Y fee-tier discount matches execute ([GitLab **#238**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/238)). **Off-chain callers** (dapp preflight, indexer route solve) must forward the connected wallet as `trader` on every LCD hop when quoting for a registered tier holder ([GitLab **#245**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/245)). Sim queries are read-only (no deregister side-effects). **Bounded query gas:** the discount registry is queried **at most once per quote** — forward sim resolves the effective fee then calls the core `simulate_hybrid_swap_with_fee`; `HybridReverseSimulation` resolves it **once before** its exponential+binary search and reuses it across all iterations (never per-iteration), so reverse quotes for discounted traders stay within the LCD smart-query gas cap (#238 guardrail). On-chain check: [`scripts/qa/verify-issue-238.sh`](../scripts/qa/verify-issue-238.sh) (`make verify-issue-238`). Router `SimulateSwapOperations` forwards the same optional fields per hop. With non-zero `book_input`, quotes walk the limit book read-only (see `docs/limit-orders.md`); parity vs execute assumes the same chain snapshot (queries do not mutate expired head cleanup). Indexer **`GET /api/v1/route/solve`** (hybrid-aware **by default** when `amount_in` is set — [GitLab **#191**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/191); legacy **`pool_only=true`**) and **`POST`** (`hybrid_by_hop`) merge hybrid into router ops and LCD `simulate_swap_operations` when `ROUTER_ADDRESS` is set; optional query/body **`trader`** / **`sender`** forwarded to all pair-level `HybridSimulation` calls and router sim (**#245**). **Multihop:** per-hop `pool_input + book_input` must equal that hop’s simulated offer amount; 3+ hop paths with hybrid on multiple legs: [GitLab **#192**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/192). | [pair `simulate_hybrid_swap`](../smartcontracts/contracts/pair/src/contract.rs); [router sim](../smartcontracts/contracts/router/src/contract.rs); [indexer `route_solver`](../indexer/src/api/route_solver.rs); [ADR 0001](./adr/0001-hybrid-quoting-and-routing.md) | `limit_order_tests::router_simulate_swap_hybrid_matches_pool_when_book_empty`, `hybrid_forward_sim_matches_execute_when_book_empty`, `hybrid_reverse_pool_only_template_is_stable`, `hybrid_pool_and_book_legs_one_swap`, `router_two_hop_first_leg_hybrid_matches_simulate`, **`router_three_hop_two_legs_hybrid_matches_simulate`**, **`hybrid_simulation_matches_execute_with_fee_discount`**; indexer `api_route_solve` (incl. `route_solve_post_three_hop_multi_leg_hybrid`, `route_solve_get_hybrid_optimize_three_hops`, **`route_solve_get_default_hybrid_two_hops`**, **`route_solve_get_with_trader_returns_higher_estimate`**); [limit-orders.md](./limit-orders.md); [skills/AGENTS_HYBRID_QUOTING.md](../skills/AGENTS_HYBRID_QUOTING.md); [`skills/AGENTS_TESTING_MULTIHOP_HYBRID.md`](../skills/AGENTS_TESTING_MULTIHOP_HYBRID.md) |


## Limit orders and hybrid swaps (pair)

See invariant rows **L1–L10** above and [`limit-orders.md`](./limit-orders.md) for message shapes, pause semantics, indexer hints, and simulation limits.

## Attack paths considered (non-governance)

- Unauthorized factory/pair/hook/discount admin calls → blocked (`Unauthorized`).
- Discount theft via fake `trader` from non-trusted router → blocked (query uses `sender`).
- Sandwich / flash-style value extraction → mitigated by fees + `max_spread` / slippage tests (`security_tests`, proptest).
- Fee-on-transfer / reserve imbalance → documented; sweep recovers excess balance without editing reserves.
- Hook griefing (reverting hook) → intentional if governance registered an allowlisted hook that errors; tested (`audit_invariant_tests::swap_fails_atomically_when_allowlisted_hook_reverts` — full tx rollback; pool unchanged). Disallowed caller → `new_feature_tests::swap_with_reverting_hook_fails` + `hooks_integration_tests::test_*_unauthorized_hook_caller_rejected` (burn / tax / lp-burn).
- Router `SWAP_STATE` after a failed tx → not stuck; `router_coverage_tests::test_router_failed_swap_then_retry_succeeds`.
- Router dust / attribution → `adversarial_token::router_ignores_pre_existing_dust_on_output_token`, `adversarial_token::router_multi_hop_ignores_dust_on_intermediate_and_final_tokens` (R4 / [#240](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/240)).

## Residual risks (not “bugs” under trusted governance)

- **Malicious governance** can set destructive hooks, pause pairs (blocking swaps, new limit placements, limit cancels, limit price updates, and **parked-expiry claims** until unpause), or point discount registry to broken contracts (users pay full fee if query fails). Parked `EXPIRED_LIMIT_CLAIMS` rows remain claimable only after unpause (GitLab [**#120**](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/120)).
- **Wasm admin / migration** on-chain is outside these crates; deployment checklist should restrict migration keys.
- **Indexer / frontend** are not authoritative for on-chain safety; oracle/TWAP consumers must follow disclaimers in `dex-common` pair query docs.

## Third-party audit

[security-model.md](./security-model.md) notes that a formal third-party audit is recommended before high-TVL mainnet. This document does **not** replace an external audit.

## Maintenance

When adding a new execute path or economic rule:

1. Add a row to the invariant matrix (or extend an existing row).
2. Add a **deterministic** regression test that encodes the business rule (not only line coverage).
3. If the property is numeric or sequence-based, add or extend a **proptest** in `cl8y-dex-tests`.
4. Run `cargo test` in `smartcontracts/` and, for coverage, see [testing.md](./testing.md) (Rust / LLVM coverage).

