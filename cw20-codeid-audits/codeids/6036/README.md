# 6036 — listed TerraSwap cw20-base control

Already on factory whitelist (PEARL/QUARTZ). Exceptions vs 10184 (if any) belong in `REPORT.md` after `CODE_ID=6036` fetch + decomp + suite.

LCD crate fingerprint from #581 notes: `cw20_base::msg::QueryMsg` (no `balance_at` / `tax_map`).
