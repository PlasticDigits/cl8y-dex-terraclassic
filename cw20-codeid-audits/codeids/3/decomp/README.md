# Generated decompile output (wat / c / objdump / strings).

Run `./cw20-codeid-audits/scripts/decompile-wasm.sh 3` after fetch. Not redistributable source — decompilation of LCD wasm.

Fingerprint JSON is committed. `cw20_legacy`, `interface_version_7`, and `addr_canonicalize` must be **true**. `tax_map` must be **false**.
