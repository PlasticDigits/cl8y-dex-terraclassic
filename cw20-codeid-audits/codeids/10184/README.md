# 10184 — protocol mintable control

Factory-listed PlasticDigits `cw20-mintable`. Honest Layer A/B in-process analogue: `cw20_mintable` in `cw20_codeid_harness` (does not require LCD).

Fetch LCD wasm when running `CODE_ID=10184 make verify-issue-589`. A check that fails here is a **harness bug**, not a listing veto.
