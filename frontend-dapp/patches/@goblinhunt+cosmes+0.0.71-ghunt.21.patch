diff --git a/node_modules/@goblinhunt/cosmes/dist/wallet/wallets/keplr/KeplrExtension.js b/node_modules/@goblinhunt/cosmes/dist/wallet/wallets/keplr/KeplrExtension.js
index bcfd177..cca2429 100644
--- a/node_modules/@goblinhunt/cosmes/dist/wallet/wallets/keplr/KeplrExtension.js
+++ b/node_modules/@goblinhunt/cosmes/dist/wallet/wallets/keplr/KeplrExtension.js
@@ -1,7 +1,88 @@
 import { RpcClient, Tx, } from "../../../client/index.js";
+import { CosmosTxV1beta1AuthInfo as ProtoAuthInfo, } from "../../../protobufs/index.js";
 import { WalletType } from "../../constants/WalletType.js";
 import { ConnectedWallet, } from "../ConnectedWallet.js";
 import { WalletError } from "../WalletError.js";
+/** Per-request sign options — Station LocalTerra may ignore defaultOptions alone (GitLab #127). */
+const EXTENSION_SIGN_OPTIONS = {
+    preferNoSetFee: true,
+    preferNoSetMemo: true,
+};
+const EXTENSION_SIGNED_FEE_UNDERSHOOT_PREFIX = "Wallet signed a fee far below what this dApp submitted (GitLab #127).";
+const EXTENSION_SIGNED_FEE_MIN_PERCENT = 95n;
+function extensionSignedFeeUndershootError(expectedUluna, expectedGas, signedUluna, signedGas) {
+    const gasNote = expectedGas > 0n && signedGas > 0n && signedGas < expectedGas
+        ? ` Expected gas at least ~${expectedGas}; wallet returned ~${signedGas}.`
+        : "";
+    return new Error(`${EXTENSION_SIGNED_FEE_UNDERSHOOT_PREFIX} On LocalTerra with Station: disconnect, reconnect, and approve any chain-update prompt. Run \`cd frontend-dapp && npm ci\` so the cosmes patch is applied, then retry. Expected at least ~${expectedUluna} uluna; wallet returned ~${signedUluna} uluna.${gasNote}`);
+}
+function meetsMinSignedRatio(signed, expected) {
+    if (expected <= 0n)
+        return true;
+    if (signed <= 0n)
+        return false;
+    return signed * 100n >= expected * EXTENSION_SIGNED_FEE_MIN_PERCENT;
+}
+/** Reject broadcast when a wallet rewrites fees/gas far below the dApp `Fee` (Station LocalTerra repro: ~3000 uluna; #134 ~23 vs ~36 LUNC). */
+function ulunaFromAminoFee(fee) {
+    const coin = fee?.amount?.find((c) => c.denom === "uluna");
+    return coin?.amount ? BigInt(coin.amount) : 0n;
+}
+function gasFromAminoFee(fee) {
+    const g = fee?.gas;
+    if (g == null || g === "")
+        return 0n;
+    try {
+        return BigInt(g);
+    }
+    catch {
+        return 0n;
+    }
+}
+function gasLimitFromProtobufFee(fee) {
+    return fee?.gasLimit ? BigInt(fee.gasLimit) : 0n;
+}
+function ulunaFromDirectSignedAuthInfoBytes(authInfoBytes) {
+    if (!authInfoBytes?.length)
+        return 0n;
+    const auth = ProtoAuthInfo.fromBinary(authInfoBytes);
+    const coin = auth.fee?.amount?.find((c) => c.denom === "uluna");
+    return coin?.amount ? BigInt(coin.amount) : 0n;
+}
+function gasFromDirectSignedAuthInfoBytes(authInfoBytes) {
+    if (!authInfoBytes?.length)
+        return 0n;
+    const auth = ProtoAuthInfo.fromBinary(authInfoBytes);
+    return auth.fee?.gasLimit ? BigInt(auth.fee.gasLimit) : 0n;
+}
+function assertExtensionSignedFeeMeetsExpected(signedDoc, expectedFee, chainId) {
+    const isLocalTerra = typeof chainId === "string" && chainId.toLowerCase() === "localterra";
+    if (!isLocalTerra)
+        return;
+    const expectedUluna = ulunaFromAminoFee(expectedFee);
+    const expectedGas = gasLimitFromProtobufFee(expectedFee);
+    if (expectedUluna <= 0n && expectedGas <= 0n)
+        return;
+    if (!signedDoc?.fee)
+        throw extensionSignedFeeUndershootError(expectedUluna, expectedGas, 0n, 0n);
+    const signedUluna = ulunaFromAminoFee(signedDoc.fee);
+    const signedGas = gasFromAminoFee(signedDoc.fee);
+    if (!meetsMinSignedRatio(signedGas, expectedGas) || !meetsMinSignedRatio(signedUluna, expectedUluna))
+        throw extensionSignedFeeUndershootError(expectedUluna, expectedGas, signedUluna, signedGas);
+}
+function assertExtensionSignedDirectFeeMeetsExpected(signed, expectedFee, chainId) {
+    const isLocalTerra = typeof chainId === "string" && chainId.toLowerCase() === "localterra";
+    if (!isLocalTerra)
+        return;
+    const expectedUluna = ulunaFromAminoFee(expectedFee);
+    const expectedGas = gasLimitFromProtobufFee(expectedFee);
+    if (expectedUluna <= 0n && expectedGas <= 0n)
+        return;
+    const signedUluna = ulunaFromDirectSignedAuthInfoBytes(signed.authInfoBytes);
+    const signedGas = gasFromDirectSignedAuthInfoBytes(signed.authInfoBytes);
+    if (!meetsMinSignedRatio(signedGas, expectedGas) || !meetsMinSignedRatio(signedUluna, expectedUluna))
+        throw extensionSignedFeeUndershootError(expectedUluna, expectedGas, signedUluna, signedGas);
+}
 export class KeplrExtension extends ConnectedWallet {
     constructor(walletName, label, ext, chainId, pubKey, address, rpc, gasPrice, useAmino) {
         super(walletName, WalletType.EXTENSION, label, chainId, pubKey, address, rpc, gasPrice);
@@ -43,11 +124,15 @@ export class KeplrExtension extends ConnectedWallet {
         };
         let txRaw;
         if (this.useAmino) {
-            const { signed, signature } = await WalletError.wrap(this.ext.signAmino(this.chainId, this.address, tx.toStdSignDoc(params)));
-            txRaw = tx.toSignedAmino(signed, signature.signature);
+            const stdDoc = tx.toStdSignDoc(params);
+            const { signed, signature } = await WalletError.wrap(this.ext.signAmino(this.chainId, this.address, stdDoc, EXTENSION_SIGN_OPTIONS));
+            // Single sign only — a second signAmino after approval often yields "User denied, extension popup was closed" (GitLab #208).
+            assertExtensionSignedFeeMeetsExpected(signed, fee, this.chainId);
+            txRaw = tx.toSignedAmino(signed, signature.signature);
         }
         else {
-            const { signed, signature } = await WalletError.wrap(this.ext.signDirect(this.chainId, this.address, tx.toSignDoc(params)));
+            const { signed, signature } = await WalletError.wrap(this.ext.signDirect(this.chainId, this.address, tx.toSignDoc(params), EXTENSION_SIGN_OPTIONS));
+            assertExtensionSignedDirectFeeMeetsExpected(signed, fee, this.chainId);
             txRaw = tx.toSignedDirect(signed, signature.signature);
         }
         return RpcClient.broadcastTx(this.rpc, txRaw);
diff --git a/node_modules/@goblinhunt/cosmes/dist/wallet/wallets/luncdash/LUNCDashController.js b/node_modules/@goblinhunt/cosmes/dist/wallet/wallets/luncdash/LUNCDashController.js
index 641a1e0..37c6857 100644
--- a/node_modules/@goblinhunt/cosmes/dist/wallet/wallets/luncdash/LUNCDashController.js
+++ b/node_modules/@goblinhunt/cosmes/dist/wallet/wallets/luncdash/LUNCDashController.js
@@ -50,17 +50,22 @@ export class LUNCDashController extends WalletController {
         const address = wc.accounts[0];
         for (let i = 0; i < chains.length; i++) {
             const { chainId, rpc, gasPrice } = chains[i];
+            let key;
             try {
-                // Since Station's WalletConnect doesn't support getting pub keys, we
-                // need to query the account to get it. However, if the wallet does
-                // not contain funds, the RPC will throw errors.
-                const key = await WalletError.wrap(this.getPubKey(chainId, rpc, address));
-                wallets.set(chainId, new LUNCDashWalletConnectV1(undefined, wc, chainId, key, address, rpc, gasPrice));
+                key = await WalletError.wrap(this.getPubKey(chainId, rpc, address));
             }
             catch (err) {
-                // We simply log and ignore the error for now
-                console.warn(err);
+                // Account may not have a pub key on-chain yet (no outgoing tx).
+                // Use a placeholder — LuncDash WalletConnect handles signing in the
+                // wallet app, so the pub key is not needed for transactions.
+                console.warn(`[LUNCDash] Could not fetch on-chain pub key for ${address} on ${chainId}. ` +
+                    `Using placeholder; wallet will still function via WalletConnect.`, err);
+                key = new Secp256k1PubKey({
+                    chainId,
+                    key: new Uint8Array(33),
+                });
             }
+            wallets.set(chainId, new LUNCDashWalletConnectV1(undefined, wc, chainId, key, address, rpc, gasPrice));
         }
         this.wc.cacheSession(wc);
         return { wallets, wc: this.wc };
diff --git a/node_modules/@goblinhunt/cosmes/dist/wallet/wallets/station/StationController.js b/node_modules/@goblinhunt/cosmes/dist/wallet/wallets/station/StationController.js
index 021a61a..bbf719c 100644
--- a/node_modules/@goblinhunt/cosmes/dist/wallet/wallets/station/StationController.js
+++ b/node_modules/@goblinhunt/cosmes/dist/wallet/wallets/station/StationController.js
@@ -49,17 +49,22 @@ export class StationController extends WalletController {
         const address = wc.accounts[0];
         for (let i = 0; i < chains.length; i++) {
             const { chainId, rpc, gasPrice } = chains[i];
+            let key;
             try {
-                // Since Station's WalletConnect doesn't support getting pub keys, we
-                // need to query the account to get it. However, if the wallet does
-                // not contain funds, the RPC will throw errors.
-                const key = await WalletError.wrap(this.getPubKey(chainId, rpc, address));
-                wallets.set(chainId, new StationWalletConnectV1(undefined, wc, chainId, key, address, rpc, gasPrice));
+                key = await WalletError.wrap(this.getPubKey(chainId, rpc, address));
             }
             catch (err) {
-                // We simply log and ignore the error for now
-                console.warn(err);
+                // Account may not have a pub key on-chain yet (no outgoing tx).
+                // Use a placeholder — Station WalletConnect handles signing in the
+                // wallet app, so the pub key is not needed for transactions.
+                console.warn(`[Station] Could not fetch on-chain pub key for ${address} on ${chainId}. ` +
+                    `Using placeholder; wallet will still function via WalletConnect.`, err);
+                key = new Secp256k1PubKey({
+                    chainId,
+                    key: new Uint8Array(33),
+                });
             }
+            wallets.set(chainId, new StationWalletConnectV1(undefined, wc, chainId, key, address, rpc, gasPrice));
         }
         this.wc.cacheSession(wc);
         return { wallets, wc: this.wc };
@@ -79,7 +84,10 @@ export class StationController extends WalletController {
                     key: pubKey,
                     chainId,
                 });
-                wallets.set(chainId, new StationExtension(this.id, name, ext, chainId, key, bech32Address, rpc, gasPrice, isNanoLedger));
+                // GitLab #127 / #208: Station's Keplr shim mishandles signDirect (fee rewrite, false "User denied").
+                // Always use amino for the extension path; Ledger already required amino in upstream cosmes.
+                const useAminoSigning = true;
+                wallets.set(chainId, new StationExtension(this.id, name, ext, chainId, key, bech32Address, rpc, gasPrice, useAminoSigning));
             }
             catch (err) {
                 if (err instanceof Error) {
diff --git a/node_modules/@goblinhunt/cosmes/src/wallet/wallets/keplr/KeplrExtension.ts b/node_modules/@goblinhunt/cosmes/src/wallet/wallets/keplr/KeplrExtension.ts
index eb54188..845a813 100644
--- a/node_modules/@goblinhunt/cosmes/src/wallet/wallets/keplr/KeplrExtension.ts
+++ b/node_modules/@goblinhunt/cosmes/src/wallet/wallets/keplr/KeplrExtension.ts
@@ -23,6 +23,15 @@ import {
 import { WalletError } from "../WalletError";
 import { AddChainInfo } from "cosmes/wallet/walletconnect/WalletConnectV2";
 
+/**
+ * Pass on every `signAmino` / `signDirect` call. Station on LocalTerra can ignore
+ * `defaultOptions.sign` alone and rebuild fees from a stale gas step (GitLab #127).
+ */
+const EXTENSION_SIGN_OPTIONS = {
+  preferNoSetFee: true,
+  preferNoSetMemo: true,
+} as const;
+
 export class KeplrExtension extends ConnectedWallet {
   private readonly ext: Keplr;
   private readonly useAmino: boolean;
@@ -99,12 +108,22 @@ export class KeplrExtension extends ConnectedWallet {
     let txRaw: TxRaw;
     if (this.useAmino) {
       const { signed, signature } = await WalletError.wrap(
-        this.ext.signAmino(this.chainId, this.address, tx.toStdSignDoc(params))
+        this.ext.signAmino(
+          this.chainId,
+          this.address,
+          tx.toStdSignDoc(params),
+          EXTENSION_SIGN_OPTIONS
+        )
       );
       txRaw = tx.toSignedAmino(signed, signature.signature);
     } else {
       const { signed, signature } = await WalletError.wrap(
-        this.ext.signDirect(this.chainId, this.address, tx.toSignDoc(params))
+        this.ext.signDirect(
+          this.chainId,
+          this.address,
+          tx.toSignDoc(params),
+          EXTENSION_SIGN_OPTIONS
+        )
       );
       txRaw = tx.toSignedDirect(signed, signature.signature);
     }
diff --git a/node_modules/@goblinhunt/cosmes/src/wallet/wallets/luncdash/LUNCDashController.ts b/node_modules/@goblinhunt/cosmes/src/wallet/wallets/luncdash/LUNCDashController.ts
index 707d1df..fb101ac 100644
--- a/node_modules/@goblinhunt/cosmes/src/wallet/wallets/luncdash/LUNCDashController.ts
+++ b/node_modules/@goblinhunt/cosmes/src/wallet/wallets/luncdash/LUNCDashController.ts
@@ -64,29 +64,37 @@ export class LUNCDashController extends WalletController {
     const address = wc.accounts[0];
     for (let i = 0; i < chains.length; i++) {
       const { chainId, rpc, gasPrice } = chains[i];
+      let key: Secp256k1PubKey;
       try {
-        // Since Station's WalletConnect doesn't support getting pub keys, we
-        // need to query the account to get it. However, if the wallet does
-        // not contain funds, the RPC will throw errors.
-        const key = await WalletError.wrap(
+        key = await WalletError.wrap(
           this.getPubKey(chainId, rpc, address)
         );
-        wallets.set(
-          chainId,
-          new LUNCDashWalletConnectV1(
-            undefined,
-            wc,
-            chainId,
-            key,
-            address,
-            rpc,
-            gasPrice
-          )
-        );
       } catch (err) {
-        // We simply log and ignore the error for now
-        console.warn(err);
+        // Account may not have a pub key on-chain yet (no outgoing tx).
+        // Use a placeholder — LuncDash WalletConnect handles signing in the
+        // wallet app, so the pub key is not needed for transactions.
+        console.warn(
+          `[LUNCDash] Could not fetch on-chain pub key for ${address} on ${chainId}. ` +
+          `Using placeholder; wallet will still function via WalletConnect.`,
+          err
+        );
+        key = new Secp256k1PubKey({
+          chainId,
+          key: new Uint8Array(33),
+        });
       }
+      wallets.set(
+        chainId,
+        new LUNCDashWalletConnectV1(
+          undefined,
+          wc,
+          chainId,
+          key,
+          address,
+          rpc,
+          gasPrice
+        )
+      );
     }
     this.wc.cacheSession(wc);
     return { wallets, wc: this.wc };
diff --git a/node_modules/@goblinhunt/cosmes/src/wallet/wallets/station/StationController.ts b/node_modules/@goblinhunt/cosmes/src/wallet/wallets/station/StationController.ts
index d372281..e88620d 100644
--- a/node_modules/@goblinhunt/cosmes/src/wallet/wallets/station/StationController.ts
+++ b/node_modules/@goblinhunt/cosmes/src/wallet/wallets/station/StationController.ts
@@ -63,29 +63,37 @@ export class StationController extends WalletController {
     const address = wc.accounts[0];
     for (let i = 0; i < chains.length; i++) {
       const { chainId, rpc, gasPrice } = chains[i];
+      let key: Secp256k1PubKey;
       try {
-        // Since Station's WalletConnect doesn't support getting pub keys, we
-        // need to query the account to get it. However, if the wallet does
-        // not contain funds, the RPC will throw errors.
-        const key = await WalletError.wrap(
+        key = await WalletError.wrap(
           this.getPubKey(chainId, rpc, address)
         );
-        wallets.set(
-          chainId,
-          new StationWalletConnectV1(
-            undefined,
-            wc,
-            chainId,
-            key,
-            address,
-            rpc,
-            gasPrice
-          )
-        );
       } catch (err) {
-        // We simply log and ignore the error for now
-        console.warn(err);
+        // Account may not have a pub key on-chain yet (no outgoing tx).
+        // Use a placeholder — Station WalletConnect handles signing in the
+        // wallet app, so the pub key is not needed for transactions.
+        console.warn(
+          `[Station] Could not fetch on-chain pub key for ${address} on ${chainId}. ` +
+          `Using placeholder; wallet will still function via WalletConnect.`,
+          err
+        );
+        key = new Secp256k1PubKey({
+          chainId,
+          key: new Uint8Array(33),
+        });
       }
+      wallets.set(
+        chainId,
+        new StationWalletConnectV1(
+          undefined,
+          wc,
+          chainId,
+          key,
+          address,
+          rpc,
+          gasPrice
+        )
+      );
     }
     this.wc.cacheSession(wc);
     return { wallets, wc: this.wc };
