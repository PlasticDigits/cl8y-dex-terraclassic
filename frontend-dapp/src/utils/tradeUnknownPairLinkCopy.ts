/** Retail copy for `/trade/:pairAddr` deep links that look like Terra addresses but are not on this factory ([GitLab #175](https://gitlab.com/PlasticDigits/cl8y-dex-terraclassic/-/issues/175)). */

export const TRADE_UNKNOWN_PAIR_LINK_TITLE = 'Pair not found'

export const TRADE_UNKNOWN_PAIR_LINK_LEAD =
  'This link does not match a pair on this DEX. Select a pair from the list or check the address in the URL.'

export const TRADE_UNKNOWN_PAIR_LINK_CTA = 'Select a trading pair'
