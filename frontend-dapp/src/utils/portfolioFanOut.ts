/** Max indexed pairs scanned for portfolio LP overview (LCD balance fan-out). */
export const PORTFOLIO_LP_MAX_PAIRS = 50

/** Concurrent LCD balance queries when building portfolio LP overview. */
export const PORTFOLIO_LP_CONCURRENCY = 5

/** Default page size for wallet-wide open limits on `/portfolio`. */
export const PORTFOLIO_OPEN_LIMITS_DEFAULT_LIMIT = 100
