/**
 * Primary money-action buttons on trade routes — matches Swap CTA visual weight (GitLab #417).
 * Swap reference: `py-3.5 sm:py-4 text-base`; trade tickets use `py-3 text-sm` minimum.
 */
export const TRADE_MONEY_CTA_CLASS = 'btn-primary btn-cta w-full py-3 text-sm font-semibold'

/** Slippage preset chips on the market panel — ~44px touch target (GitLab #417). */
export const TRADE_SLIPPAGE_PRESET_CLASS = 'tab-glass min-h-11 px-3 py-2 text-sm'
